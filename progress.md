# AutoAgent Harness Progress

## 2026-04-29

- Created persistent planning index files for the AutoAgent harness redesign.
- Current canonical design note remains `docs/superpowers/specs/2026-04-28-auto-agent-main-loop-harness-working-notes.md`.
- Current branch is intended for package rename and future harness refactor work.
- Package root rename from `cn.bugstack.ai` to `yhx.com` has been completed and test-compiled, but not committed.
- Started Phase 2.1 draft design for `AgentState`, `StateView`, and `StateDelta`.
- Started Phase 2.2 draft design for Runtime loop state machine.
- Confirmed tool execution separation: MainAgentNode emits `CALL_TOOL`, Runtime delegates to dedicated ToolExecutionNode with MCP tools.
- Confirmed Runtime loop state machine direction, including dedicated ToolExecutionNode path.
- Added design rule that refined designs must cover all cases and complete JSON/action structures.
- Started Phase 3 draft design for database persistence and repository boundaries.
- User delegated Phase 3 database/repository detailed design to assistant judgment; keep chat concise and carry full details into working notes/final spec.
- Confirmed Phase 4 DDD module/package layout and configuration layering.
- Confirmed ContextPlannerNode location and boundary: Runtime is Java orchestration, ContextPlannerNode is a lightweight LLM node used before MainAgentNode to select context.
- Confirmed recovery design: Runtime uses guard/verifier structured errors plus recovery templates; no ad hoc LLM inside Runtime.
- Confirmed ToolExecutionNode contract refinement: expectedOutcome must support verification while tool-specific success details come from capability configuration.
- Confirmed FinalResponseGuard MVP scope: Java rule-based pipeline now, LLM/special safety guards later.
- Confirmed prompt/contract storage principle: Java owns harness protocol; database owns editable prompt content.
- Confirmed Phase 5 scope: prompt/contract architecture and boundaries are accepted now; final prompt text and full JSON schemas will be produced during final spec consolidation.
- Confirmed Phase 6 frontend/API direction and required SSE emitter event streaming.
- Confirmed Phase 7 testing strategy: minimal critical backend tests and frontend mock scenarios with SSE event streams.
- Performed pre-spec consistency audit; latest decisions override older draft notes, especially ToolExecutionNode replacing direct MainAgentNode Spring AI tool-use.
- Started final spec consolidation phase with strict requirement that spec becomes the sole coding reference and implementation handbook.
- Created canonical English spec and temporary Chinese review sample with sections 0-2 and full chapter skeleton.
- Filled spec chapters 3-4 for Runtime state machine and AgentState/StateView/StateDelta, with Chinese review sample synchronized.
- Filled spec chapters 5-6 for node contracts/prompts and verification/guard pipeline, with Chinese review sample synchronized.
- Expanded node operational prompts and replaced abbreviated Chinese MainAgent action section with complete action JSON.
- Review finding: node prompts were too role-level and Chinese sample omitted full MainAgent action JSON. Expanding prompts and synchronizing complete action structures before continuing.
- Added NodeInvocationPipeline and ContractRegistry to English spec, expanded layered prompt envelope, and fixed section numbering after prompt-depth review.
- Filled spec chapters 7-11 up to testing: persistence, DDD layout, capability/tool configuration, frontend API/SSE, and logging/trace/audit.
- Confirmed Chinese review sample must be a full translation of the English spec, not a shortened summary.
- Corrected Section 5 prompt/contract design after review: every LLM node now goes through NodeInvocationPipeline, ContractRegistry resolves node-specific contracts, prompt assembly uses layered envelopes, Runtime materializes ContextPlanner refs into MainAgentStateView, and Section 5 action headings are synchronized through 5.23 in both English spec and Chinese review sample.
- Filled English spec Section 12 Testing Strategy with minimal-but-real test policy, required backend tests, API/SSE tests, frontend mock scenarios, deterministic fixtures, execution policy, and safety properties.
- Completed remaining English spec sections 13-14 with staged implementation plan and backlog governance. Added matching Chinese review content for sections 12-14 so the user can review testing, implementation phases, and backlog in Chinese.
- Applied first review correction pass to the AutoAgent main-loop spec: narrowed MVP ToolVerifier to execution proof, added pending input interruption model and UserInputResolverNode, added ContextPlannerStatus handling, changed RagVerifier to LLM grounding-honesty verification, forced all finalAnswerCandidate paths through FinalResponseGuard, unified UserVisibleEvent/SSE DTOs, added agent_pending_input and agent_node_prompt persistence, scoped CapabilityRegistry to external tools only, and fixed English example mojibake.
- Integrated the first review corrections into the Chinese review sample's main chapters as well: Section 3 now includes ContextPlannerStatus handling and Pending Input/user reply lifecycle, Section 4 includes UserInputResolverInput, Section 5 includes UserInputResolverNode prompt and UserInputResolution contract, and the action routing/final guard/tool execution wording now matches the English canonical spec.
- Started second spec review. Fixed three consistency issues: ToolVerifier examples now use `TOOL_RECEIPT_MISSING` instead of business-result mismatch, runtime test paths now use defined RuntimePhase names, and RecoveryPolicy now maps every MVP failure code to a deterministic recovery action. Noted that the Chinese review sample still has summary-only references in sections 7-11 and should be fully translated before spec lock if the Chinese sample remains required for review.
- Completed focused review of the English canonical spec as the implementation source of truth. Fixed run lifecycle wording for `WAITING_USER`, clarified that `ContractRegistry` owns structured output contracts for both LLM nodes and Java-only verifier outputs, and added `FINAL_REPAIR` / `CONTRACT_REPAIR` to prompt storage node codes. Mechanical checks show failure codes map to recovery actions and MainAgent actions map to StateDelta write scopes. No remaining hard contradiction found in the English spec.
- Reviewed `ultraworkers/claw-code` as an external clean-room/reference implementation for one-loop agent design. Main candidate impact is changing tool execution from an LLM `ToolExecutionNode` to deterministic Runtime-owned MCP invocation with permission enforcement, typed tool receipts, and replay-safe transcript blocks. Awaiting user approval before modifying the canonical spec.
- Applied accepted `claw-code`-inspired spec changes: English canonical spec now replaces `ToolExecutionNode` with Runtime-owned `ToolRuntime` backed by Spring AI MCP clients, adds typed run transcript persistence and compaction invariants, strengthens MainAgentNode prompt layers with concrete behavior/risk/untrusted-content/final-answer rules, updates capability/yml/persistence/DDD/test/implementation sections, and synchronizes the Chinese review sample with the same high-level changes.
- Reviewed the latest spec changes. No old ToolExecutionNode naming remains in the English canonical spec. Review found follow-up fixes needed before spec lock: `agent_tool_call.tool_node_run_id` still reflects the old node model; permission/approval enums and persistence fields are underdefined; ToolArgumentMaterializer reference syntax is inconsistent (`contentRef` vs `contentSource`); implementation Phase 1 enum list omits new tool/permission/transcript enums; Chinese review sample is still mojibake-heavy and summary-style in parts.
- Fixed the four English canonical spec blockers from review: replaced `tool_node_run_id` with Runtime `tool_invocation_id` plus MCP metadata fields; added typed permission model, decision shape, approval/workspace/destructive rules, yml capability fields, and persistence columns; standardized ToolArgumentMaterializer references on `contentSource` / `evidenceSource`; added missing Phase 1 value objects and enums for transcript, permission, approval, MCP transport, and tool argument sources. Follow-up scan found no old ToolExecutionNode naming in the English spec.
- Completed another English-first spec review pass. Patched additional consistency issues: ContractRegistry wording now uses component code rather than node-only code, the prompt layer table includes the newly introduced stable/boundary/untrusted/risk layers, ToolInvocationStatus and ToolCallStatus now cover permission denied and unavailable/invalid tool states, ToolVerifier and RecoveryPolicy now map ToolRuntime failure states, and yml defaults include UserInputResolverNode and RagVerifier model config. Chinese review sample received a latest-change summary and a small terminology alignment, but remains non-authoritative.
- Started resolving the latest five spec review issues one by one. For issue 1, confirmed the unified ASK_USER design: all user-facing questions flow through Runtime and domain `UserInteractionManager`, with shared pending-input APIs, shared free-text resolution via `UserInputResolverNode`, and continuation handlers. Updated the English canonical spec with `clarificationRequest` for `ContextPlannerNode`, canonical ask request/continuation shapes, DDD service entries, tests, and implementation tasks.
- Per user instruction, added the working rule that each future spec point modification must include a cross-spec consistency scan. Re-scanned issue 1 and aligned remaining old wording: `ASK_USER`, ContextPlanner clarification, context-over-budget, tool approval, approval policy, persistence notes, and implementation tasks now route pending input through `UserInteractionManager`; `SINGLE_CHOICE_OR_FREE_TEXT` is now included in `ASK_USER` allowed input modes.
- Refined issue 2 design per user correction: user interaction normalization must not decide whether a vague answer needs another clarification. Exact option clicks are converted by Java into synthetic `RESOLVED` `UserInputResolution`; free-form replies go through `UserInputResolverNode` and return an allowed custom `RESOLVED` resolution with `rawUserText` and hints. Removed `NEEDS_CLARIFICATION` from `UserInputResolution.status` and answer contracts; follow-up questions are only created by the resumed source component through the normal ask path. Re-scanned the English spec for `NEEDS_CLARIFICATION` and confirmed remaining uses are ContextPlanner/context-clarification concepts, not user-resolution status.
- Revised issue 2 again after user corrected the model: removed the LLM-based `UserInputResolverNode` concept from the English canonical spec. User interaction is now Java-only: `UserReplyProcessor` converts option clicks, free text, or cancellation into `UserAnswer`; option clicks use stored `option.value`, free text is preserved as `FREE_TEXT`, and the resumed component decides semantic adequacy. Updated MVP scope, architecture, LLM boundaries, data ownership, runtime phase, pending input lifecycle, StateView type list, section 4.7, ContractRegistry, prompt storage node codes, DDD package layout, yml node config, persistence fields, tests, fixtures, implementation phases, and backlog references. Full scan shows no `UserInputResolverNode`, `UserInputResolverInput`, `UserInputResolution`, `USER_INPUT_RESOLVER`, `user-input-resolver`, or `answerContract` references remain in the English spec.
- Completed issue 3 tool approval spec update. Detailed design now states Runtime owns approval lifecycle, ToolRuntime only performs fail-closed permission re-check and cannot create pending input or approval records, approval uses unified `UserInteractionManager`, `approval_key` and `argumentsHash` provide idempotency, explicit approve/reject options are deterministic Java data, free-text cannot authorize high-risk execution, and `ToolApprovalPendingInputHandler` defines handling for approved/rejected/modified/cancelled/malformed user answers. Updated `agent_tool_approval` fields/indexes, repository methods, DDD entity/DAO/enums, tests, and implementation phase tasks. Cross-spec scan found no old ToolExecution/UserInputResolver remnants and no ToolRuntime-as-pending-input-source conflict remains.
- Simplified issue 3 per user correction: high-risk tool approval no longer allows free-text input. `TOOL_APPROVAL` uses `SINGLE_CHOICE` with `allowFreeText=false`; only explicit approve/reject options are valid. Removed MVP `MODIFIED` approval status/decision and `markApprovalModified`; free-text approval is invalid and cannot authorize execution. Updated API example, tests, implementation tasks, and approval status definitions. Re-scanned the spec and remaining free-text references are for non-approval user clarification/context questions.
- Rechecked `inputMode` alignment across spec after user asked about frontend design. Fixed `ASK_USER` example from `SINGLE_CHOICE` + `allowFreeText=true` to `SINGLE_CHOICE_OR_FREE_TEXT`. Added frontend rendering rules for every `inputMode`, including backend rejection of free text for `SINGLE_CHOICE`, explicit approve/reject-only UI for high-risk `TOOL_APPROVAL`, and mock scenarios for `ask_user_choice_or_text` and `tool_approval_no_text`. Re-scanned for obsolete resolver/tool-execution terms and input mode conflicts.
- Revised issue 4 per user correction: RagVerifier invocation is now fact-triggered only. Runtime sets `RagState.ragWasUsed=true` and persists `agent_run.rag_was_used=1` when `RETRIEVE_RAG` is accepted/executed; final-answer keyword or claim detection no longer triggers RagVerifier in MVP. `claimsKnowledgeBaseGrounding` remains a verifier input field only. Updated routing, `RagVerifierInput`, persistence, recovery wording, tests, and Phase 7 tasks; follow-up scan is required before moving to the next issue.
- Completed issue 4 RagVerifier spec expansion. Added `RagVerifierInput` as a Runtime-built view with bounded evidence summaries/snippets, expanded RagVerifier prompt with role/stable rules/input guide, added a decision matrix, failure-code mapping, example `VerificationResult`, explicit non-goals, stronger RecoveryPolicy wording, DDD/phase value-object references, and broader RagExecutionAndVerificationTest coverage. The accepted trigger rule is fact-based only: `RagState.ragWasUsed=true`.
- Completed the final-delivery/debug-observability spec update after user confirmation. Added strict normal frontend vs debug data boundary, expanded `FinalResponseGuard` into a full final delivery path with `FinalResponseGuardInput`, `FinalResponseGuardResult`, `FinalResponse`, action-specific final delivery rules, failure codes, and bounded repair. Added `DebugDataPipeline`, expanded `agent_run_trace` fields/types, added debug SSE/API rules, yml debug fail-closed switches, DDD value objects/services/DAOs, debug data tests, API/SSE tests, and implementation phase tasks. Normal UI remains limited to clean events and guarded final responses; debug/run process data is persisted for future visualization and automated log analysis.
- Completed English canonical spec unification pass after global review, excluding the Chinese sample by user request. Fixed `FINAL_RAW_JSON_LEAK` by adding it to global failure codes and RecoveryPolicy, added `FINAL_RESPONSE_GUARD` to ContractRegistry, clarified `VerificationResult` versus `FinalResponseGuardResult`, removed the `FAIL` final-answer bypass by routing failure text through the final delivery path, and expanded Phase 0 configuration properties to include `auto-agent.rag`, `auto-agent.mcp`, and debug switches.
- Completed lightweight spec-lock review for the English canonical spec. Verified all JSON code blocks parse successfully, all MainAgent actions have JSON examples and StateDelta scopes, main LLM node prompts are populated with role/stable rules/operational instructions/input guides/decision policies/examples, and contract mappings exist for Runtime components. Fixed one small status naming inconsistency by standardizing ToolInvocationResult no-call status to `NOT_CALLED` while keeping `TOOL_NOT_CALLED` as the failure code.
- Started development planning. Created the master implementation plan at `docs/superpowers/plans/2026-05-12-auto-agent-main-loop-harness-master-plan.md`, splitting the full harness redesign into Phase 0-12 and identifying the immediate next detailed plan as Phase 0/1 contract skeleton.
- Created the executable Phase 0/1 implementation plan at `docs/superpowers/plans/2026-05-12-auto-agent-phase-0-1-contract-skeleton.md`. It covers package skeleton, typed configuration, enums, value objects, entities, contract registry, raw output parser, contract validator, recovery mapping, targeted tests, acceptance checks, and optional subagent split guidance.
- Created the executable Phase 2 persistence/repository plan at `docs/superpowers/plans/2026-05-12-auto-agent-phase-2-persistence-repository.md`. It covers SQL DDL, PO/DAO/MyBatis mapper files, domain repository interfaces, infrastructure repository adapters, DB-backed payload storage, normal UI versus debug data separation, repository boundary tests, and optional subagent split guidance.
- Created the executable Phase 3 prompt/node-invocation plan at `docs/superpowers/plans/2026-05-12-auto-agent-phase-3-prompt-node-invocation.md`. It covers layered prompt assembly, Java-owned stable prompt fragments, component prompt builders, output contract rendering, node client port, fake-client test flow, NodeInvocationPipeline, bounded contract repair, Spring AI adapter boundary, and prompt/contract tests.
- Created the executable Phase 4 context/artifact/memory plan at `docs/superpowers/plans/2026-05-12-auto-agent-phase-4-context-artifact-memory.md`. It covers compact candidate preselection, ContextPlanner invocation wrapper, ContextPlannerStatus handling, context budget management, artifact resolution and loading policy, context materialization into MainAgentStateView, artifact create/update helpers, memory/evidence helpers, scenario expectations, tests, and optional subagent split guidance.
- Created the executable Phase 5 runtime/pending-input plan at `docs/superpowers/plans/2026-05-12-auto-agent-phase-5-runtime-pending-input.md`. It covers Runtime state machine, phase transitions, loop policy, recovery counters, same-run pending input pause/resume, Java-only user answer normalization, continuation dispatch, transcript/event/trace boundaries, safe failure creation, tests, and optional subagent split guidance.
- Created the executable Phase 6 main-action-handlers plan at `docs/superpowers/plans/2026-05-12-auto-agent-phase-6-main-action-handlers.md`. It covers the production action dispatcher, one handler per MainAgentAction, handler ports for final delivery/RAG/tool/plan state, artifact create/update routing, ASK_USER through UserInteractionManager, ragWasUsed setting, CALL_TOOL orchestration boundary, final text routing, tests, and optional subagent split guidance.
- Created the executable Phase 7 RAG runtime/verification plan at `docs/superpowers/plans/2026-05-12-auto-agent-phase-7-rag-runtime-verification.md`. It covers explicit RETRIEVE_RAG execution, ragWasUsed fact persistence, query/hit/payload/evidence storage, bounded RagVerifierInput building, RagVerifier invocation through NodeInvocationPipeline, fact-based verifier routing, RAG recovery mapping, tests, and optional subagent split guidance.
- Created the executable Phase 8 tool/MCP/permission/approval plan at `docs/superpowers/plans/2026-05-12-auto-agent-phase-8-tool-mcp-permission-approval.md`. It covers capability registry, MCP registries and adapters, permission decisions, approval idempotency, explicit option-only tool approval, argument materialization, ToolRuntime, receipt capture, ToolVerifier execution-proof validation, tool evidence, transcript records, tests, and optional subagent split guidance.
- Created the executable Phase 9 final-response-guard/repair plan at `docs/superpowers/plans/2026-05-12-auto-agent-phase-9-final-response-guard-repair.md`. It covers the single final delivery path, Java guard chain, empty/internal/raw-json/format/citation/tool-claim/length checks, RAG verification integration point, REPAIR_FINAL invocation, repair budgets, fixed safe fallback, final response persistence, tests, and optional subagent split guidance.
- Created the executable Phase 10 API/SSE/debug/mock plan at `docs/superpowers/plans/2026-05-12-auto-agent-phase-10-api-sse-debug-mock.md`. It covers chat/run/final/pending/artifact APIs, mandatory normal SSE, isolated debug APIs and debug SSE, DebugDataPipeline, mock frontend scenarios, DTO safety boundaries, API/SSE tests, and optional subagent split guidance.
- Created the executable Phase 11 old-harness isolation/cleanup plan at `docs/superpowers/plans/2026-05-12-auto-agent-phase-11-old-harness-isolation-cleanup.md`. It covers caller audit, normal route replacement, legacy switch defaults, old Node1-4 output removal from normal APIs/SSE, old prompt/parser quarantine or deletion, node-trace isolation, isolation tests, and optional subagent split guidance.
- Created the executable Phase 12 MVP verification/review plan at `docs/superpowers/plans/2026-05-12-auto-agent-phase-12-mvp-verification-review.md`. It covers scenario fixtures, targeted test command matrix, safety-property tests, API/SSE/debug checks, old-harness isolation verification, verification report template, known-gaps backlog template, defect severity rules, and optional subagent split guidance.
- Confirmed implementation execution rules before coding: use a dedicated feature branch, save meaningful Git checkpoints, record branch/commit/verification results in `progress.md`, and implement by vertical slices rather than mechanically following Phase 0-12 order. Updated the master implementation plan and `task_plan.md` with the vertical-slice-first development strategy and checkpoint policy.
- Started implementation on branch `feature/auto-agent-main-loop-harness`. First vertical slice is `checkpoint-01-foundation-contracts`: Java-owned contract registry, raw output parser, contract validator, StateDelta scope rules, core run/action/context enums, typed AutoAgent configuration properties, and package-root migration baseline. Verification passed with `mvn -q -pl ai-agent-station-study-domain -am -DskipTests compile` and `mvn -q -pl ai-agent-station-study-app -am "-Dtest=MainAgentActionContractTest,ContextPlannerContractTest,AutoAgentPropertiesTest" "-Dsurefire.failIfNoSpecifiedTests=false" test` (14 tests passed). Git checkpoint commit: `e4094f3` (`agent: checkpoint foundation contracts`).
- Implemented the second vertical slice: minimal direct-answer Runtime path. Added `AutoAgentRuntimeService`, `DefaultAutoAgentRuntimeService`, `MainAgentNodePort`, `RuntimeStartCommand`, `RuntimeResult`, and Java-only `FinalResponseGuard`. The slice supports fake MainAgentNode `FINAL` output, validates the action through `ContractValidator`, rejects internal final-answer leakage, and returns a guarded `RuntimeResult`. Verification passed with `mvn -q -pl ai-agent-station-study-app -am "-Dtest=AutoAgentDirectRuntimeSliceTest,MainAgentActionContractTest,ContextPlannerContractTest,AutoAgentPropertiesTest" "-Dsurefire.failIfNoSpecifiedTests=false" test` (16 tests passed). Git checkpoint commit: `4261ab6` (`agent: add direct runtime slice`).
- Implemented the third cleanup/DDD alignment slice. Deleted the old fixed Node1-4 execute chain, old auto-agent API/controller DTOs, old step/token/session-memory tests, and old Node1-4-only state/value objects. Kept armory and moved reusable contract/tool/token value objects to DDD-aligned `model/valobj` packages: `contract`, `tool`, and `token`. `service/contract` now only contains behavior-oriented services such as registry/parser/validator/rules/policies. Verification passed with `mvn -q -pl ai-agent-station-study-domain -am -DskipTests compile`, `mvn -q -pl ai-agent-station-study-app -am "-Dtest=AutoAgentDirectRuntimeSliceTest,MainAgentActionContractTest,ContextPlannerContractTest,AutoAgentPropertiesTest" "-Dsurefire.failIfNoSpecifiedTests=false" test` (16 tests passed), and `mvn -q -pl ai-agent-station-study-app -am -DskipTests compile`. Git checkpoint commit: `8f8a49b` (`agent: remove legacy execute harness`).
- Implemented the fourth DDD package normalization slice. Renamed external RAG contract to `IRagApi` and domain RAG contract to `IRagDomainService`. Moved domain model objects into subdomain-aligned folders: `entity/armory`, `entity/rag`, `entity/memory`, `valobj/armory`, `valobj/runtime`, and enum folders `enums/armory`, `enums/contract`, `enums/context`, `enums/runtime`. Removed unused old step/round status enums from the active source tree. Verification passed with `mvn -q -pl ai-agent-station-study-domain -am -DskipTests compile`, `mvn -q -pl ai-agent-station-study-app -am -DskipTests compile`, and `mvn -q -pl ai-agent-station-study-app -am "-Dtest=AutoAgentDirectRuntimeSliceTest,MainAgentActionContractTest,ContextPlannerContractTest,AutoAgentPropertiesTest,RagServiceTest" "-Dsurefire.failIfNoSpecifiedTests=false" test` (20 tests passed). Git checkpoint commit: `09beb99` (`agent: normalize ddd package layout`).
- Added source-encoding cleanup checkpoint after user found mojibake in `RAGController`. Cleaned Java source/test mojibake in RAG, armory assembly, auto configuration, armory value objects, and `AgentRepository`; historical SQL seed files remain intentionally untouched because the table structure will be replaced later. Verification passed with Java mojibake `rg` scan, `git diff --check`, and `mvn -q -pl ai-agent-station-study-app -am -DskipTests compile`. Git checkpoint commit: `5d09538` (`agent: clean source mojibake`).
- Started `checkpoint-03-persistence-boundary` with the first persistence foundation slice. Added domain persistence entities for session, run, message, payload, artifact, evidence, pending input, event, trace, and audit; added persistence enums for message role, payload type, event type, trace type, and audit type; added domain repository interfaces for run, conversation, payload, artifact, evidence, pending input, event/trace, and transcript boundaries. Added an in-memory test fixture proving normal conversation reads do not expose debug trace payloads. Verification passed with `mvn -q -pl ai-agent-station-study-domain -am -DskipTests compile` and `mvn -q -pl ai-agent-station-study-app -am "-Dtest=PersistenceBoundaryTest,AutoAgentDirectRuntimeSliceTest" "-Dsurefire.failIfNoSpecifiedTests=false" test` (5 tests passed). Git checkpoint commit: `4cd3252` (`agent: add persistence boundary contracts`).
- Continued `checkpoint-03-persistence-boundary` with the core infrastructure persistence slice. Added complete new harness DDL at `docs/dev-ops/mysql/sql/auto-agent-main-loop-harness.sql` with 23 runtime tables; old historical SQL remains untouched. Added core PO/DAO/MyBatis mapper files and infrastructure repository adapters for payload, session/message, run, artifact, evidence, pending input, and event/trace/audit. Verification passed with `mvn -q -pl ai-agent-station-study-domain -am -DskipTests compile`, `mvn -q -pl ai-agent-station-study-app -am -DskipTests compile`, `mvn -q -pl ai-agent-station-study-app -am "-Dtest=PersistenceBoundaryTest,AutoAgentDirectRuntimeSliceTest" "-Dsurefire.failIfNoSpecifiedTests=false" test` (5 tests passed), and `git diff --check`. Git checkpoint commit: `d0f882b` (`agent: add core persistence infrastructure`).
- Completed the remaining `checkpoint-03-persistence-boundary` persistence infrastructure slice. Added domain contracts/entities/enums plus PO/DAO/MyBatis mapper/repository adapters for conversation summaries, long-term memory, memory events, tool calls, tool approvals, tool verification, RAG queries, RAG hits, node prompts, and typed run transcript blocks. Verification passed with `mvn -q -pl ai-agent-station-study-domain -am -DskipTests compile`, `mvn -q -pl ai-agent-station-study-app -am -DskipTests compile`, `mvn -q -pl ai-agent-station-study-app -am "-Dtest=PersistenceBoundaryTest,AutoAgentDirectRuntimeSliceTest" "-Dsurefire.failIfNoSpecifiedTests=false" test` (5 tests passed), and `git diff --check`. Git checkpoint commit: `3625eff` (`agent: add remaining persistence adapters`).
- Implemented `checkpoint-04-prompt-node-invocation`. Added Java-owned layered prompt assembly, static/repository role prompt providers, component prompt builders for ContextPlanner/MainAgent/RagVerifier/repair, output contract rendering, `INodeClientPort`, node invocation command/result/attempt objects, typed output mapper, `NodeInvocationPipeline`, bounded contract repair flow, and a Spring AI adapter skeleton that keeps Spring AI out of the domain. Tightened `RawOutputParser` so ordinary prose before/after JSON can no longer bypass the output-only contract; fenced JSON remains supported. Verification passed with `mvn -q -pl ai-agent-station-study-domain -am -DskipTests compile`, `mvn -q -pl ai-agent-station-study-infrastructure -am -DskipTests compile`, `mvn -q -pl ai-agent-station-study-app -am -DskipTests compile`, `mvn -q -pl ai-agent-station-study-app -am "-Dtest=PromptAssemblerTest,NodeInvocationPipelineTest,ContractRepairPipelineTest,PromptContractBoundaryTest,MainAgentActionContractTest,ContextPlannerContractTest" "-Dsurefire.failIfNoSpecifiedTests=false" test` (27 tests passed), required `rg` cross-spec scans, and `git diff --check`. Git checkpoint commit: `0da8870` (`agent: add prompt invocation pipeline`).
