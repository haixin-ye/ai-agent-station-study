# AutoAgent 项目面试复习手册

> **用途**：围绕项目真实实现整理高频面试问题、关键机制、实现边界与可直接口述的回答。
>
> **阅读方式**：先阅读每题的“给面试官的回答”建立主线，再回看具体实现和追问补充细节。

## 阅读目录

1. **01**　Runtime 的状态机和核心数据结构是什么？
2. **02**　Main Node 如何创建、并发和管理多个子 Agent？
3. **03**　一次 MCP 工具调用的完整流程是怎样的？
4. **04**　Main Node 的上下文是如何设计的？
5. **05**　MCP 工具调用失败后如何记录、回传和重试？
6. **06**　为什么选择“单 Main Node 循环 + Runtime 执行器”，而不是固定多节点工作流？
7. **07**　Main Node 输出的结构化 Action 不合法时如何处理？

---

## 01. Runtime 的状态机和核心数据结构是什么？

### 01.1. 项目中的具体实现

#### 01.1.1. 先说结论

&emsp;&emsp;项目里的 Runtime 不是一个抽象概念，也不是由 LLM 自由驱动的工作流。它是一个确定性的 Java 编排服务，负责创建一次任务运行记录、推进合法状态、调用主 Agent、路由 Action、保存过程数据，以及处理暂停、恢复、失败和最终交付。

&emsp;&emsp;一次用户请求会创建一个 `Run`。Run 可以理解为这次请求在后端的完整执行实例，它拥有自己的状态、当前阶段、循环次数、任务计划、每轮执行记录和最终结果。

#### 01.1.2. 两层状态设计

&emsp;&emsp;Runtime 同时维护两层状态：

1. **RunStatus：任务的宏观状态**
   - `CREATED`：任务已创建。
   - `RUNNING`：正在执行。
   - `WAITING_USER`：等待用户补充信息或授权。
   - `WAITING_CHILDREN`：等待子 Agent 完成。
   - `COMPLETED`、`FAILED`、`CANCELLED`：完成、失败或取消。

2. **RuntimePhase：当前执行到哪一步**
   - 上下文阶段：准备候选内容、规划上下文、构建状态视图。
   - 模型阶段：调用主 Agent、校验 Action、必要时修复输出。
   - 执行阶段：路由 Action、执行 RAG、准备和调用工具、验证工具结果。
   - 交互阶段：等待用户、解析用户回答、等待子 Agent。
   - 交付阶段：验证最终回答、修复回答、完成或失败。

&emsp;&emsp;`RunStatus` 回答“任务现在总体处于什么状态”，`RuntimePhase` 回答“任务具体执行到哪一步”。例如，一个 Run 的宏观状态可以是 `RUNNING`，当前阶段则是 `INVOKING_TOOL_RUNTIME`；暂停后宏观状态变成 `WAITING_USER`，当前阶段也进入 `WAITING_USER`。

&emsp;&emsp;状态迁移由 `RuntimeStateMachine` 控制。它内部保存一张合法迁移表，例如“构建状态视图”只能进入“调用主 Agent”，“工具调用”之后必须进入“验证工具结果”。任何不在表中的跳转都会被拒绝并转为安全失败，LLM 不能直接修改状态或随意跳转。

#### 01.1.3. 主循环

&emsp;&emsp;`DefaultAutoAgentRuntimeService` 是总编排器，主要由启动、恢复和循环执行三部分组成：

- `start`：创建 Run、保存用户消息、初始化运行上下文。
- `runLoop`：反复构建上下文、调用主 Agent、校验并执行 Action。
- `resume` / `resumeChildren`：用户回答或子 Agent 完成后，从持久化状态恢复执行。

&emsp;&emsp;主循环可以简化为：

```text
创建 Run
  -> 准备并筛选上下文
  -> 构建 MainAgent 可见的状态视图
  -> 调用 MainAgent
  -> 校验结构化 Action
  -> 根据 Action 路由到 RAG、工具、ASK_USER、子 Agent 或最终交付
  -> 保存本轮结果
  -> 继续下一轮、暂停等待，或者结束
```

&emsp;&emsp;Runtime 每轮开始前检查最大循环次数。Action 执行后，只有结果明确要求继续时才进入下一轮；需要用户或子 Agent 时会持久化状态并退出当前执行线程；完成、失败或取消时结束 Run。

#### 01.1.4. 核心数据结构

| 数据结构 | 含义 | 主要内容 |
| --- | --- | --- |
| `AgentRunEntity` | 数据库中的 Run 主记录 | runId、sessionId、userId、宏观状态、当前阶段、是否使用过 RAG、最终回答引用、失败码 |
| `RuntimeExecutionContext` | 当前 Java 执行过程中的运行对象 | 用户输入、当前状态和阶段、循环次数、恢复计数器、最近 Action、运行时临时事实 |
| `RunContextStateVO` | 可持久化的任务工作状态 | 基础上下文、任务账本、运行控制信息、每轮时间线 |
| `TaskLedgerVO` | 任务账本 | 总目标、交付物、执行步骤、当前步骤、计划修订、事实和阻塞项 |
| `RunLoopRecordVO` | 单轮执行记录 | 本轮序号、主 Agent 输出、Action 请求、执行结果、影响的步骤和证据、开始结束时间 |
| `MainAgentStateViewVO` | 本轮提供给主 Agent 的有界快照 | 用户输入、精选会话、记忆、RAG、工具结果证据、可用能力、待处理动作和 Token 预算 |
| `MainAgentActionVO` | 主 Agent 返回的结构化决策 | Action 类型、任务计划更新、需要 Runtime 执行的状态增量 |

&emsp;&emsp;这里需要区分三个容易混淆的对象：

- `RuntimeExecutionContext` 是当前进程执行时使用的“工作台”。
- `RunContextStateVO` 是可以保存并在重启后恢复的“任务工作档案”。
- `MainAgentStateViewVO` 是从任务档案中筛选后提供给 LLM 的“有限视图”，并不包含所有内部数据。

### 01.2. 补充知识

&emsp;&emsp;状态机的核心价值是把“模型的不确定决策”和“系统的确定执行”分开。主 Agent 可以决定下一步想做什么，但能否执行、如何执行、执行后进入哪个阶段，都由 Java Runtime 决定。

&emsp;&emsp;因此，本项目不是固定 Workflow：任务步骤不是预先写死的；它也不是完全自治的 LLM：模型不能绕过状态、权限和校验规则。它属于“LLM 负责语义决策，Runtime 负责生命周期和系统控制”的 Agent 架构。

### 01.3. 给面试官的回答

&emsp;&emsp;我们把一次用户请求建模为一个可持久化的 Run，并使用两层状态管理。第一层是 RunStatus，表示任务正在运行、等待用户、等待子 Agent、完成或失败；第二层是 RuntimePhase，表示当前具体处在上下文准备、模型调用、Action 校验、工具执行、结果验证还是最终交付阶段。

&emsp;&emsp;Runtime 本质上是一个 Java 编排服务。它通过合法状态迁移表和主循环推进任务：先构建有界状态视图，调用主 Agent 获取一个结构化 Action，再由 Runtime 校验并路由到 RAG、工具、ASK_USER、子 Agent 或最终交付。每轮的 Action、执行结果、任务计划和证据都会写入可持久化的 RunContextState；如果需要等待用户或子 Agent，就保存现场并退出，满足恢复条件后重新加载状态继续运行。

&emsp;&emsp;核心数据包括 Run 主记录、当前执行上下文、持久化任务状态、任务账本、单轮执行记录、给模型看的 State View，以及模型返回的 Action。这个设计的关键是让 LLM 只负责语义决策，而状态推进、外部执行、异常恢复和最终交付始终由确定性的 Runtime 控制。

---

## 02. Main Node 如何创建、并发和管理多个子 Agent？

### 02.1. 项目中的真实实现

&emsp;&emsp;这条链路的起点不是 Runtime 看到任务复杂后自动拆分，而是 Main Node 在一次决策中输出 `DELEGATE_AGENTS` Action。Main Node 会结合用户目标、当前任务账本、已有执行结果、可用工具和上下文信息进行语义判断。例如，一个任务同时包含“搜集多个来源”和“分别处理不同文件”两个相互独立的部分，并且每个部分都有清晰的输入、边界和交付物时，就适合委派给多个子 Agent。当前实现没有一个由 Java 编写的“复杂度评分器”来替代模型判断；Runtime 负责校验模型的委派请求是否完整、合法，而不是重新猜测任务是否复杂。

&emsp;&emsp;委派 Action 中包含一个批次的等待模式和多个子任务。每个子任务不是一句模糊的“去完成它”，而是一个受约束的任务描述，主要包括：任务 ID、名称、目标、边界、期望输出、请求使用的能力，以及允许传给子 Agent 的初始上下文。名称用于识别和展示，目标说明要解决什么问题，边界限制不能做什么，期望输出规定父 Agent 最后要拿到什么，能力列表则说明是否需要检索、调用工具或访问文件等能力。Runtime 会把请求的能力与通用子 Agent 的能力配置、工作区范围做交集，得到实际允许使用的能力；模型不能通过文字自行扩大权限。

&emsp;&emsp;接下来，负责“登记父子关系”的调度组件会为每个任务生成独立的子 `Run`，并记录父 Run、子 Run、任务 ID、批次 ID 和当前关系状态。这个关系记录相当于一张任务清单，后面 Runtime 可以据此判断哪些子任务已经完成、哪些失败、父 Run 是否具备恢复条件。子 Run 使用统一的通用子 Agent 模板：它拥有自己的执行循环、自己的上下文记录、自己的 Action 校验和自己的 Action 处理器，但可以复用同一套 Java 节点实现、Prompt 构建器和 LLM 客户端。

&emsp;&emsp;这里的“复用同一个 LLM 节点”不等于多个子 Agent 串行执行。调度器会把每个子任务分别提交给子 Agent 执行器；每次调用都有独立的 `childRunId`、任务输入和上下文，因此多个子 Agent 可以同时发起各自的 LLM 请求和工具请求。共享的是无状态的代码和模型客户端，不共享正在变化的工作状态。真实的并发数量由执行器线程池、模型服务并发能力、网络和工具耗时共同决定，而不是由子 Agent 数量无限决定。

&emsp;&emsp;父 Run 也不会一直占用一个线程等待子任务。委派动作完成登记并启动子任务后，父 Run 被持久化为 `WAITING_CHILDREN`，当前执行方法返回。每个子 Run 结束时，调度关系会被更新为 `COMMITTED`、`FAILED` 或 `BLOCKED`。当前首要等待模式是 `WAIT_ALL`，也就是所有子任务进入终态后，才触发父 Run 恢复；恢复时不是从头开始，而是加载父 Run 的持久化状态、委派关系和子任务结果，从“等待子 Agent”之后重新进入主循环。

&emsp;&emsp;子 Agent 不会把一大段未经整理的内部上下文直接塞回父 Agent。子 Agent 结束时提交一个结构化结果，包含任务 ID、结果状态、结果内容、细节说明、证据引用、检查过的资源、假设、阻塞原因和建议父 Agent 下一步如何处理。结果投影组件会把这些内容写入父 Run 对应的委派记录，并合并证据引用。下一轮构建 State View 时，父 Agent 看到的是这些结构化的子任务结果，于是可以判断：接受结果并继续、根据失败信息重新规划、补派任务、向用户询问，或者直接最终交付。

### 02.2. 上下文和 Runtime 是否独立

&emsp;&emsp;每个子 Agent 都有独立的 Runtime 执行实例和独立的上下文容器。子上下文以追加记录的方式保存任务说明、每轮 Action、工具或 RAG 的处理结果、运行备注以及最终提交结果。创建子 Agent 时，父 Run 只显式传递任务需要的初始上下文、有效能力和可用工具描述，不直接共享父 Run 的可变工作状态，也不把父 Agent 的完整 State View 原样复制过去。这样既能隔离不同子任务，也能控制子 Agent 的输入规模。

&emsp;&emsp;因此，子 Agent 的“独立”主要是运行状态和上下文独立，不代表每个子 Agent 都必须部署一个新的模型服务。它们可以调用相同的模型接口，但请求中的任务描述、能力范围、子 Run 标识和上下文不同。子 Agent 仍然运行在同一套通用循环中，达到循环上限、提交结果、等待用户或发生不可恢复错误时结束本次执行。

### 02.3. 失败、超时和结果质量

&emsp;&emsp;当前实现对可确定的失败有明确处理：子 Agent 输出不符合 Action 合约、请求了不允许的能力、工具或 RAG 处理异常、执行器拒绝任务、子 Agent 超过最大循环次数时，都会把子 Run 标记为失败；子 Agent 如果需要用户补充信息，则进入等待用户状态，而不是被误判为失败。失败结果会写入父子关系和父 Run 的委派结果中，不会被静默丢弃。父 Run 在满足等待条件后恢复，Main Node 可以基于失败原因重新规划或决定整体失败。

&emsp;&emsp;需要准确区分当前实现和生产补强项：当前代码已经有循环上限和异常失败回传，但子 Agent 调度层还没有完整的独立墙钟超时监控，某个底层模型请求长期不返回时，父 Run 可能持续处于等待状态；同时，当前主要验证子 Agent 提交结果的结构和字段完整性，没有一个独立的语义质量评估器自动判断“文章是否足够好”。工程化补强应增加每个子 Run 的截止时间和 watchdog、模型与工具调用超时、有限重试和幂等键，并为不同任务定义确定性验收规则或独立验证节点。质量不足时只在预算内重试或重新委派，超过预算则以部分结果、明确阻塞原因或失败状态交给父 Agent，而不是无限循环。

### 02.4. 必要的补充理解

&emsp;&emsp;这个设计解决的是两个问题。第一，Main Agent 不必亲自完成所有步骤，可以把天然可并行的工作拆开，提高复杂任务的吞吐。第二，子 Agent 不是写死的“搜索 Agent”或“文件 Agent”，而是由任务描述和能力边界临时塑造的通用执行单元，因此更接近一个可复用的通用 Agent 底座。

&emsp;&emsp;它的代价也很明确：并发会增加模型调用、工具调用和状态管理成本；父 Agent 必须处理子任务失败、部分成功和结果冲突；如果没有超时、幂等和质量验收，系统容易出现长期等待、重复执行或把低质量结果继续向下游传播。因此面试时应同时说明现有实现和下一步的工业化治理方案。

### 02.5. 给面试官的回答

&emsp;&emsp;当主 Agent 判断一个任务可以拆分为多个边界清晰、相互独立的子任务时，它会输出一个委派 Action。这个 Action 不是只包含“创建几个 Agent”，而是为每个子任务给出任务 ID、名称、目标、执行边界、期望输出和请求的能力。Runtime 会校验这些字段，并把请求能力与系统允许的能力和工作区范围做交集，生成每个子 Agent 实际可用的权限，避免模型自行扩大范围。

&emsp;&emsp;Runtime 随后为每个子任务创建独立的子 Run，记录父子关系和批次信息，并通过执行器分别提交，所以多个子 Agent 可以并发执行。它们复用同一套通用 Agent 节点和 Runtime 代码，但每个实例拥有独立的 Run 标识、上下文、任务输入和能力边界；父 Run 不会阻塞占用线程等待，而是持久化为等待子任务状态。

&emsp;&emsp;子 Agent 完成后提交结构化结果，包括成功或失败状态、结果内容、证据、假设、阻塞原因和建议。Runtime 在所有子任务进入终态后恢复父 Run，把结果投影到父 Run 的委派记录和下一轮 State View 中，由 Main Agent 决定继续执行、重新规划、补派任务还是最终交付。当前实现已经覆盖结构校验、能力校验、异常回传和循环上限；对于完整生产化，还需要补充子 Run 墙钟超时、有限重试、幂等控制和独立结果质量验收，防止长期等待、重复执行和低质量结果扩散。

### 02.6. 追问：部分子 Agent 成功、部分失败时怎么办？

&emsp;&emsp;假设一次委派创建了三个子 Agent，其中两个成功提交，第三个执行失败。当前实现不会把两个成功结果丢掉，也不会因为第三个失败就直接把父 Run 判为失败。每个子任务都有自己的关系状态，`COMMITTED`、`FAILED` 和 `BLOCKED` 都属于终态；当前委派使用 `WAIT_ALL`，因此只要三个子任务都进入终态，父 Run 的等待条件就满足。

&emsp;&emsp;父 Run 被唤醒后，结果投影组件会把三个结果分别写入父 Run 的委派结果中。成功任务会包含结果内容和证据引用，失败任务会包含失败状态和失败原因。父 Agent 下一轮看到的是一个完整的部分结果集合，而不是只有一段混合文本：

```text
任务 A -> SUCCESS + result + evidence
任务 B -> SUCCESS + result + evidence
任务 C -> FAILED  + failureMessage
```

&emsp;&emsp;此时系统不会在 Runtime 中直接替父 Agent 做最终语义判断。Main Node 会根据失败任务是否关键来决定后续动作：如果两个成功结果已经足够，可以继续整合；如果缺少的任务是关键环节，可以重新规划或只重新委派失败任务；如果整体目标已经无法可靠完成，则返回失败或请求用户补充信息。

&emsp;&emsp;当前实现没有“子任务失败后自动重新创建同一个子 Agent”的固定重试流程。重新执行通常需要父 Agent 在恢复后的下一轮输出新的委派 Action，Runtime 再创建新的子 Run。这样做的好处是可以结合失败原因重新调整目标、能力或参数，代价是重试决策依赖 Main Node，不能把所有失败都机械重做。

&emsp;&emsp;还要区分两种超时。第一种是模型或工具调用本身有超时，调用返回异常后，子 Agent 可以把该任务标记为 `FAILED`，父 Run 按上述部分失败流程恢复。第二种是整个子 Run 长时间没有返回，例如底层请求卡住或执行线程异常占用；当前实现的子 Agent 调度层还缺少完整的墙钟超时 watchdog，这种情况下父 Run 可能一直处于 `WAITING_CHILDREN`。生产化时应由独立监控器在截止时间到达后把子任务明确标记为 `TIMEOUT` 或 `FAILED`，持久化失败原因并主动触发父 Run 恢复。

&emsp;&emsp;对于“结果质量差”，当前主要保证结果结构完整、状态合法、任务身份匹配，尚没有一个独立的语义质量评估器自动判定结果是否足够好。因此，父 Agent 会看到结果和证据，并在下一轮进行语义判断。更完整的工程方案是为任务定义验收条件或增加独立验证节点，在预算范围内触发重试或重新委派，超过次数后保留部分成功结果并明确告知阻塞原因。

#### 02.6.1. 这个追问的面试回答

&emsp;&emsp;我们采用的是“终态汇总、父 Agent 再决策”的方式。以三个子 Agent 为例，两个成功、一个失败时，失败子任务会被标记为 `FAILED`，但它和成功状态一样都属于终态。在当前 `WAIT_ALL` 模式下，三个任务都进入终态后，父 Run 就会被唤醒；Runtime 将两个成功结果、证据以及失败任务的错误原因一起投影到父 Run 的下一轮状态视图中。

&emsp;&emsp;父 Agent 再根据任务重要性决定后续动作：可以直接使用已有结果继续完成，也可以只重新委派失败的子任务，或者判断整体任务无法可靠完成而结束。当前实现不会在 Runtime 中无条件自动重试整个子任务，因为重新执行可能造成重复的外部副作用；重试需要结合失败原因、幂等控制和父 Agent 的重新规划。

&emsp;&emsp;如果是工具或模型调用自身超时并返回异常，系统可以按失败任务处理；但如果整个子 Run 长时间不返回，当前实现还需要通过墙钟超时监控补齐状态闭环。生产环境中我会增加子 Run deadline、watchdog、有限重试、幂等键和结果验收机制，保证父 Run 不会无限等待，也不会把低质量或重复执行的结果直接传播下去。

---

## 03. 一次 MCP 工具调用的完整流程是怎样的？

### 03.1. 项目中的真实流程

&emsp;&emsp;项目没有把 MCP Client 直接交给模型自由调用。模型只能从 Runtime 提供的工具目录中选择工具并输出结构化 `CALL_TOOL` 意图；工具连接、参数校验、权限审批、真实调用、回执保存、结果验证和下一轮回写都由 Java Runtime 控制。

#### 03.1.1. MCP Server 的连接和工具发现

&emsp;&emsp;应用启动时，MCP 配置组件遍历所有启用的 Server，并根据传输类型创建同步 Client。STDIO Server 使用本地启动命令、参数和环境变量创建子进程通信通道；SSE Server 使用服务 URL、SSE endpoint、普通 Header、Bearer Token 或 API Key 创建 HTTP/SSE 通道。两种 Client 都配置请求超时，并以 `serverId` 注册到 Client Registry 中。某个 Client 创建失败时，系统记录不可用状态并继续启动，而不是让整个应用启动失败。

&emsp;&emsp;`autoInitialize` 决定是否在 Client 创建后立即初始化。但是当前配置同时开启了 `autoDiscoverTools`，工具发现组件在调用 `listTools` 前也会确保 Client 已初始化，因此当前启用的 MCP Server 实际会在启动构建工具目录时完成协议初始化。若关闭自动发现且关闭自动初始化，则会延迟到第一次真实调用时初始化。

&emsp;&emsp;工具发现组件通过 MCP 的 `listTools` 分页读取每个 Server 暴露的工具名称、描述和 Input Schema。系统随后把服务端发现结果与本地配置合并：工具名称、描述和参数 Schema 优先使用显式配置，未配置部分继承服务端发现结果；权限模式、审批策略、风险级别、工作区范围、超时和副作用属性由本地能力配置控制。这样，MCP Server 负责说明“工具是什么、参数是什么”，项目本地配置负责说明“这个工具是否允许使用、在什么边界内使用”。

&emsp;&emsp;发现并合并后的工具进入两个注册表：工具目录保存 `serverId + toolName + description + inputSchema + availability`；能力目录保存 `capabilityCode + permission + approval + risk + workspaceScope + timeout`。如果开启自动能力注册，系统会为服务端新发现且可用、又没有显式能力配置的工具生成能力编码；显式能力配置仍然优先。

#### 03.1.2. 工具如何暴露给 Main Node

&emsp;&emsp;首轮准备上下文时，工具能力投影组件从两个注册表生成 `availableCapabilities`，经过预算裁剪后写入 `SelectedSessionContext`。后续普通 Loop 不会重新执行完整的工具发现和 State View 规划，而是通过每轮构造的 `RunContextEnvelope.runtimeControl.availableCapabilities` 继续向 Main Node 提供这份能力目录。单个工具条目包含：

```text
capabilityCode、mcpServerCode、toolName、description
requiredArguments、inputSchema、schemaHash、schemaTruncated
requiredPermission、approvalPolicy、riskLevel
availability、availabilityReason
```

&emsp;&emsp;为了避免工具 Schema 占满上下文，投影过程限制最大工具数、单工具描述长度、Schema 深度、属性数量、单工具 Schema 字符数和全部工具字符预算；发生裁剪时仍优先保留必填字段路径。Main Node 的 Prompt 明确要求只能选择 State View 中暴露的工具，并按暴露的 Input Schema 生成参数，所以当前实现不再依赖 Few-shot 示例猜测新工具参数。

#### 03.1.3. Main Node 如何产生工具调用意图

&emsp;&emsp;Main Node 看到任务状态、历史结果和 `availableCapabilities` 后，如果判断下一步需要外部能力，会输出一个 `CALL_TOOL` Action：

```json
{
  "action": "CALL_TOOL",
  "stateDelta": {
    "toolIntent": {
      "capabilityCode": "baidu_ai_search_aisearch",
      "mcpServerCode": "baidu-ai-search",
      "toolName": "AIsearch",
      "goal": "检索当前公开资料",
      "arguments": {
        "query": "Java virtual threads scheduling model"
      }
    }
  }
}
```

&emsp;&emsp;Contract 校验要求 `CALL_TOOL` 必须携带 `toolIntent`，并要求 `capabilityCode`、`toolName`、`goal` 和 `arguments` 存在。`mcpServerCode` 是可选的路由提示；Runtime 主要通过能力编码解析出真实 Server 和 Tool，只有工具名称能唯一匹配时才允许省略具体能力编码。模型不能在 `stateDelta` 中写入 `toolReceipt`、运行状态或审计字段，因为这些字段属于 Runtime 所有。

#### 03.1.4. Runtime 如何找到并执行真实工具

&emsp;&emsp;Action 路由到工具处理器后，执行链依次完成以下工作：

```text
解析 Tool Intent
  -> 解析并确认已启用的能力
  -> 查询 MCP 工具元数据
  -> 补齐参数默认值和内容引用
  -> 检查权限、工作区和审批策略
  -> 必要时暂停为 WAITING_USER
  -> 使用完整 JSON Schema 校验最终参数
  -> 根据 serverId 取得真实 MCP Client
  -> 使用 toolName 和 arguments 发起 MCP callTool
  -> 保存原始回执并验证执行证据
```

&emsp;&emsp;Runtime 在调用前生成不可由模型指定的 `toolCallId` 和 `toolInvocationId`，保存 Tool Intent Payload，并创建状态为 `CREATED` 的 ToolCall 记录。能力不存在、被禁用、工具元数据缺失、参数不符合 Schema、权限拒绝或用户未批准时，真实 MCP Client 都不会被调用。

&emsp;&emsp;审批通过后，Tool Runtime 使用完整的服务端 Schema 对最终参数做第二次校验。校验通过才根据能力中的 `mcpServerCode` 从 Client Registry 取得 Client，以确定的 `toolName + arguments` 发起 MCP 协议调用。调用在 MCP 专用执行器中运行，并使用能力或 Server 配置的超时时间等待结果。

#### 03.1.5. 成功、失败和超时如何回到下一轮

&emsp;&emsp;成功时，MCP Adapter 从响应中提取 `isError`、文本内容和原始响应，形成包含 `called=true`、`success=true`、Server、Tool、原始结果、文本结果和耗时的回执。回执作为 Payload 保存，ToolCall 状态更新为 `SUCCEEDED`，请求和结果同时写入 Run Transcript。

&emsp;&emsp;失败被区分为不同情况：Client 不存在或执行器饱和时为 `NOT_CALLED`；MCP 返回 `isError=true`、调用异常或 Schema 校验失败时为 `FAILED`；超过工具超时时间时返回 `MCP_TOOL_TIMEOUT`。这些失败同样会保存状态、错误码、错误信息和可用回执。

&emsp;&emsp;随后验证器检查 ToolCall 记录是否存在、是否有 Runtime 生成的 invocationId、所需审批是否真实通过、调用状态是否成功，以及成功调用是否存在 receiptRef。只有这些条件同时满足才得到 `PASSED`。验证成功后生成工具证据；失败、拒绝、未执行和超时也会生成对应的失败证据。

&emsp;&emsp;工具处理器把结果作为本轮 `runtimeOutcome` 写入 Run Timeline，其中包含 `effectStatus`、Tool Intent、结果引用和 evidenceRefs，然后返回 `CONTINUE_LOOP`。Runtime 根据 `resultPayloadRef` 读取真实回执正文并放入 `resolvedPayloads`。下一轮构造 `RunContextEnvelope` 时，Main Node 会同时看到当前 TaskLedger、完整 LoopTimeline 和 ResolvedPayloads，因此可以读取上一轮 Tool Intent、真实成功或失败状态、Evidence 以及回执内容，再决定继续任务、换工具、调整参数、重新调用、询问用户或结束任务。工具失败默认不会直接让整个 Run 失败。

&emsp;&emsp;当前没有接通确定性的工具自动重试。代码虽然预留了 `toolRetryCount` 和最大工具重试策略，但实际工具流程没有消费这个计数；失败后是否再次调用主要由 Main Node 下一轮决定，并受全局最大循环次数约束。相同 Tool Intent 已经成功时，Runtime 会复用已有证据并跳过重复执行；相同调用已经被用户拒绝时也会跳过，避免重复副作用和重复审批。相同的失败调用目前可以被 Main Node 再次请求，尚缺少基于失败类型和幂等性的确定性重试策略。

### 03.2. “防止模型伪造工具调用”具体如何实现

&emsp;&emsp;模型只能产生 Tool Intent，不能产生可信工具结果。真实执行证据链由 Runtime 独立建立：

1. Runtime 自己生成 toolCallId 和 toolInvocationId，并创建数据库记录。
2. Runtime 通过已注册的 MCP Client 发起真实协议调用。
3. Adapter 保存 MCP 原始响应、文本结果、错误码和耗时为不可由模型直接写入的 receipt Payload。
4. ToolCall 表记录真实终态和 receiptRef，Transcript 记录请求与结果。
5. 验证器要求“真实调用记录 + 合法审批 + SUCCEEDED 状态 + receiptRef”同时成立，才生成 `PASSED` 工具证据。
6. 最终交付时，Tool Claim Guard 检查“已发布、已上传、文件已修改”等成功声明。如果没有任何通过验证的工具证据，最终回答会被拒绝并进入修复流程。

&emsp;&emsp;因此这里验证的是“工具确实被系统调用并返回成功回执”，不是仅凭模型文本判断成功。当前验证仍属于执行链完整性验证，并不等同于业务结果真实性验证。例如发布接口返回成功可以证明调用成功，但若要证明文章最终可公开访问，还需要针对该工具增加业务回查、资源 ID 校验或读后写验证。

### 03.3. 给面试官的回答

&emsp;&emsp;我们的 MCP 调用采用“模型产生调用意图、Runtime 确定性执行”的设计，而不是把 MCP Client 直接交给模型自由调用。接入 MCP 服务时，配置中声明连接方式和 Server 级默认治理策略：STDIO 服务配置启动命令、参数和环境变量，SSE 服务配置 URL、Endpoint 和认证 Header；权限、风险、审批、工作区和超时既可以使用 Server 默认值，也可以按具体工具覆盖。

&emsp;&emsp;应用启动后根据传输方式创建对应 Client。工具发现组件调用 MCP 协议的 `listTools` 获取工具名称、描述和 Input Schema；自动能力注册再为发现到的工具生成 capabilityCode，并把服务端工具定义与本地治理信息合并成统一能力目录。首轮上下文准备时，这份目录经过数量和 Schema 预算裁剪后写入 `SelectedSessionContext.availableCapabilities`，后续通过每轮 `RunContextEnvelope` 持续提供给 Main Node。

&emsp;&emsp;Main Node 只能输出结构化 `CALL_TOOL` Intent，包含 capabilityCode、toolName、goal 和 arguments。Runtime 不直接相信模型指定的 Server，而是根据 capabilityCode 查询本地能力注册表，解析出真实 MCP Server、Tool 和 Client，然后依次执行 Action 合约校验、能力可用性检查、参数 Schema 校验、工作区与权限检查。高风险操作会通过 ASK_USER 暂停 Run 并等待用户审批，全部通过后才使用 Client Registry 中的真实 Client 发起 `callTool`。

&emsp;&emsp;调用完成后，Runtime 保存 ToolCall 状态、自己生成的 invocationId、原始 MCP 回执、Transcript 和验证结果。验证器要求真实调用记录、合法审批、成功终态和 receiptRef 同时存在，才生成通过验证的 Evidence。这证明工具经过了真实执行链，而不是 Main Node 自己声称调用成功；但它属于执行链完整性验证，如需证明外部业务最终状态，还要增加发布后查询或读后写校验。

&emsp;&emsp;最后，Runtime 把 Tool Intent、执行状态、结果引用和 Evidence 写入本轮 LoopTimeline，并把结果引用对应的回执正文解析进 ResolvedPayloads。下一轮 Main Node 从 RunContextEnvelope 中读取当前 TaskLedger、完整 Timeline 和真实回执内容，再决定继续任务、修改参数、换工具、询问用户或准备交付。最终交付还有 Tool Claim Guard，没有通过验证的工具证据时，不允许声明“已发布、已上传或文件已修改”。

### 03.4. 回答时容易说错的地方

- 正式 Action 名称是 `CALL_TOOL`，不是 `TOOL_USE`。
- `listTools` 负责协议级工具发现，自动能力注册和权限治理由项目自己的模块完成。
- 不是每轮重新发现并把所有工具塞入 State View；工具目录首轮进入 SelectedSessionContext，后续随 RunContextEnvelope 继续可见，而且会受预算裁剪。
- Runtime 根据 capabilityCode 解析已注册能力，不允许模型通过伪造 URL 或 Server 名称调用任意服务。
- 真实执行验证证明调用链和回执存在，不天然等同于外部业务结果已经最终生效。

### 03.5. 追问：每轮是否把全部工具和全部属性都交给 Main Node？

&emsp;&emsp;不是。当前每轮 `RunContextEnvelope` 都会继续携带首轮确定的模型可见能力目录，但这份目录不是所有已发现工具的无损全集，也不是 MCP 和本地配置的全部内部属性。

&emsp;&emsp;工具能力投影首先只处理已启用的 Capability；MCP 工具还必须存在可用的工具元数据和 Input Schema，或者明确允许无 Schema。之后按 capabilityCode 排序，并受到最多 32 个工具、单工具和全部 Schema 字符、描述长度、Schema 深度、属性数及总能力字符等预算限制。首轮完整 State View 如果仍超过约 6000 个估算 Token，还会从能力列表末尾继续移除工具。因此 Main Node 最终看到的是预算内的能力子集，而不是注册表中的所有工具。

&emsp;&emsp;单个模型可见条目包含 capabilityCode、capabilityType、mcpServerCode、toolName、截断后的 description、requiredArguments、裁剪后的 inputSchema、schemaHash、schemaTruncated、requiredPermission、approvalPolicy、riskLevel、availability、availabilityReason、summary 和 enabled。URL、API Key、认证 Header、STDIO 启动命令等连接信息不会暴露给模型；workspaceScope、timeout、destructive、argumentDefaults 等完整执行治理配置也保留在 Runtime 的 CapabilitySpec 和 McpToolSpec 中，由 Runtime 调用时使用。模型看到的是“怎样申请调用”的视图，Runtime 保留的是“怎样安全执行”的完整配置。

&emsp;&emsp;普通 Loop 不会重新调用 `listTools` 或重新筛选工具，而是复用首轮 `SelectedSessionContext.availableCapabilities`。Envelope 当前又把同一列表放入 `runtimeControl.availableCapabilities`，所以每轮输入中实际存在重复携带。优点是模型始终能看到调用定义；缺点是工具较多时消耗上下文，而且 Run 期间的工具状态变化不能动态反映。后续可改为先注入轻量工具索引，再按任务相关性展开少量完整 Schema，同时动态刷新 availability，并消除 Envelope 中的重复字段。

### 03.6. 追问：工具发现、能力合并、预算裁剪和持久化分别是什么？

&emsp;&emsp;`listTools` 是 MCP 协议提供的标准工具发现能力。Client 初始化后可以从 Server 分页取得工具名称、描述和 Input Schema。因此项目支持“只配置 Server 连接信息和一组保守默认治理策略，自动接入其全部工具”：开启 `autoDiscoverTools` 获取工具，开启 `autoRegisterCapabilities` 为每个已发现工具生成 capabilityCode，并通过 Server 级默认 permission、approval、risk、workspaceScope 和 timeout 完成基础治理。这种方式不要求接入方预先枚举每个方法。

&emsp;&emsp;但是它目前属于“配置即接入、重启后生效”，还不是带热加载和管理界面的一键安装。若一个 Server 同时包含只读查询和高风险写操作，统一默认权限要么过宽、要么过严，此时仍应对特定工具增加显式能力覆盖。当前百度搜索和 CSDN Server 开启自动能力注册，但已知工具也有显式能力配置，显式配置优先；文件系统 Server 关闭自动能力注册，完全使用显式能力配置。服务端 Schema 缺失或不可靠时同样需要本地 Schema 兜底或覆盖。

&emsp;&emsp;发现结果与治理配置先形成两个对象：`McpToolSpec` 保存 server、toolName、description、inputSchema 和 availability，回答“工具是什么、怎样调用”；`CapabilitySpec` 保存 capabilityCode、permission、approval、risk、workspaceScope 和 timeout，回答“系统是否允许怎样使用”。能力目录构建和首轮上下文准备时，工具能力投影组件把二者按 server 和 toolName 关联，生成 SelectedSessionContext 中的 `CapabilityCandidate`。这个最终条目同时包含调用说明和治理元数据，Main Node 用它生成 Intent，Runtime 用原始两个注册表执行确定性校验。

&emsp;&emsp;工具目录自身按字符和结构预算裁剪，而不是精确 tokenizer 计数。当前开发配置为：最多 32 个工具；描述最多 300 字符；Schema 最大深度 5；单工具最多 40 个属性、2400 个 Schema 字符和 3200 个能力条目字符；全部 Schema 最多 12000 字符；全部工具能力最多 10000 字符；单工具最多 64 个必填参数。裁剪时优先保留必填路径，删除描述和可选字段，仍无法容纳时省略工具。

&emsp;&emsp;首轮选择出的 `selectedSessionContext` 还有第二层预算：默认最大 6000 个估算 Token，另外记录 1000 个输出预留 Token。估算方式是把对象序列化为 JSON 后按约两个字符一个 Token 估算，并非模型 tokenizer 的精确值。超预算时先把 Artifact 全文降级为摘要或分片；仍超预算时从工具列表末尾逐个移除。当前工具移除依据是既有排序后的末尾删除，不是按任务相关性重新评分。

&emsp;&emsp;需要注意，这个 6000 上限不等于后续每轮 Main Node 完整输入上限。当前 Main Node 接收的是 `RunContextEnvelope`：首轮 `selectedSessionContext` 只是其中一部分，此外还包含完整 `loopTimeline`、当前 `taskLedger`、`runtimeControl` 和 `resolvedPayloads`。这些运行历史没有再次经过上述 6000 Token 裁剪，所以长 Run 仍可能发生上下文膨胀，需要后续增加 Timeline 摘要、Payload 按需物化和总输入预算治理。

&emsp;&emsp;真实 MCP Response 通常提供 `content` 和 `isError` 等协议字段。系统不是把这些字段机械拆成 ToolCall、Receipt、Transcript 和 Evidence，而是以这份响应为原始输入，派生四类用途不同的记录：

- `agent_tool_call`：调用主索引，保存 Runtime 生成的 callId、invocationId、runId、Server、Tool、状态以及 intent、arguments、receipt 引用和 failureCode，用于状态验证、重复调用保护和审计。
- `agent_payload`：大内容仓库。Tool Intent、最终参数、原始 MCP Response、提取出的文本、错误码和耗时以 Payload 保存；Receipt 的 Payload 类型为 `TOOL_RECEIPT`。
- `agent_run_transcript`：按顺序保存 `TOOL_CALL_REQUEST` 和 `TOOL_RESULT` 两类块，内容通过 payloadRef 引用 Payload，用于完整运行轨迹、调试和后续压缩。
- `agent_tool_verification` 与 `agent_evidence`：前者保存执行链验证结论；后者保存适合后续推理的摘要、contentRef、verificationStatus 和 failureCode。

&emsp;&emsp;当前工具结果进入下一轮 Main Node 的主要路径已经改为近全量 Run 上下文，而不是每轮重新从 Evidence 中筛选一小份 State View。工具处理器先把 Tool Intent、效果状态、结果引用和完整的 `createdEvidence` 写入本轮 `runtimeOutcome`；Runtime 再把这条结果保存在完整 `loopTimeline` 中，并读取 `resultPayloadRef` 指向的 Receipt Payload，放入内存中的 `resolvedPayloads`。Run 恢复时也会遍历全部 Loop，再次解析每条结果引用。

&emsp;&emsp;下一轮 Main Agent Node 会构建 `RunContextEnvelope`，其中包含：`runBaseContext`（原始用户输入和首轮选择上下文）、当前 `taskLedger`、完整 `loopTimeline`、`runtimeControl` 和 `resolvedPayloads`。Prompt 明确要求先阅读最新 Loop 的 `runtimeOutcome`，再结合完整 Timeline 和 Payload 继续决策。因此模型能够直接看到上一轮 Tool Intent、`TOOL_SUCCEEDED` 或 `TOOL_FAILED`、结果摘要、evidenceRefs、创建出的 Evidence，以及被解析的 Receipt 内容。

&emsp;&emsp;这仍不是把所有数据库表原样注入：ToolCall、Verification 和 Transcript 记录主要保留在持久化与审计层；Main Node 获得的是完整因果 Timeline 和被 Runtime 解析出的结果 Payload。Evidence 依然用于首轮上下文、最终声明验证和证据引用，但不再是工具结果进入下一轮的唯一通道。

---

## 04. Main Node 的上下文是如何设计的？

### 04.1. 核心结论

&emsp;&emsp;当前 Main Node 每轮收到的已经不是旧式的单一 `StateView`，而是一个 `RunContextEnvelope`。可以把它理解为 Runtime 在调用模型前组装的“本次决策档案袋”，由五部分组成：

```text
RunBaseContext：原始任务和首轮筛选出的背景
+ TaskLedger：任务当前进展的结构化账本
+ LoopTimeline：从第 0 轮到当前的完整执行时间线
+ RuntimeControl：当前阶段和剩余循环预算
+ ResolvedPayloads：时间线中大结果引用对应的真实正文
= RunContextEnvelope -> Main Node
```

&emsp;&emsp;这五部分分别回答五个问题：用户最初要什么、任务现在做到哪里、前面每轮到底发生了什么、当前还允许怎样运行、被引用的大结果具体是什么。

### 04.2. 首轮背景如何形成

&emsp;&emsp;用户发起请求后，Runtime 先收集候选上下文，包括近期会话、会话任务摘要、历史摘要、长期记忆、用户偏好、RAG 召回结果、Artifact、已有 Evidence、待处理动作以及可用工具能力。上下文规划节点从候选中选择与当前任务相关的内容，物化为 `SelectedSessionContext`，同时记录上下文预算和输出合约版本。

&emsp;&emsp;`SelectedSessionContext` 被保存在 `RunBaseContext` 中。`RunBaseContext` 还保存 runId、sessionId、userId、agentId、原始 userInput 和用户后续补充的信息。它的定位是稳定的任务底稿，不是每轮执行历史。普通后续 Loop 不会再次执行整套记忆和 RAG 选择流程，而是持续复用这份首轮背景。

### 04.3. Main Node 如何知道任务当前做到哪里

&emsp;&emsp;`TaskLedger` 是任务的当前语义状态，包含：总目标、交付物及验收条件、执行步骤及依赖、当前步骤、计划修订历史、已确认事实、阻塞项和上次决策。每次 Main Node 输出 Action 时必须同时给出 `taskUpdate`。Runtime 在 Action 通过合约校验后，按稳定 ID 合并步骤和交付物更新，并递增账本版本，模型不能直接替换持久化状态。

&emsp;&emsp;因此 TaskLedger 适合回答“现在的结论是什么”，但它不是完整历史。比如它可以表示“资料检索步骤已完成、文章仍在生成”，却不会单独保留检索请求、原始结果和失败重试过程。

### 04.4. Main Node 如何知道前面每轮发生了什么

&emsp;&emsp;真正承载多轮因果历史的是完整 `LoopTimeline`。每个 Loop Record 都分开保存：

- `mainOutput`：Main Node 当时输出的 Action、taskUpdate 和 stateDelta。
- `actionRequest`：本轮交给 Runtime 的动作请求，例如 Tool Intent、RAG Request 或 ASK_USER Request。
- `runtimeOutcome`：Runtime 实际执行后的状态、错误码、摘要、结果引用、Evidence 引用、Artifact 引用和执行细节。
- `userInteraction`：暂停期间用户的回答、来源组件、恢复处理器、Checkpoint Payload 和恢复后的去向。
- 受影响的步骤和交付物、账本前后版本、重复调用保护键以及开始和结束时间。

&emsp;&emsp;这形成了“模型意图”和“系统事实”的明确分离。Main Node 不能因为自己上一轮说过“工具调用成功”就把它当成事实；下一轮必须读取 `runtimeOutcome` 中由 Runtime 写入的真实结果。Prompt 也明确要求执行阶段先读最新一条 Loop，再与当前 TaskLedger、完整 Timeline 和原始任务对照。

&emsp;&emsp;所有 Action 共用同一种 Loop Record，不是每种 Action 各有一张 Timeline 表。模型请求统一保存在 `mainOutput`，其中有 `action`、`taskUpdate` 和 `stateDelta`；`actionRequest` 是本轮 `stateDelta` 的运行请求视图。执行事实统一保存在 `runtimeOutcome`，用户暂停和恢复信息另外保存在 `userInteraction`。具体路径如下：

| Action | 请求字段 | 结果字段 |
|---|---|---|
| `CALL_TOOL` | `mainOutput.stateDelta.toolIntent`、`actionRequest.toolIntent` | `runtimeOutcome.status/summary/resultPayloadRef/evidenceRefs`；`runtimeOutcome.details.effectStatus/toolIntent/createdEvidence/failureMessage` |
| `RETRIEVE_RAG` | `mainOutput.stateDelta.ragRequest`、`actionRequest.ragRequest` | `runtimeOutcome.status/summary/evidenceRefs`；`runtimeOutcome.details.effectStatus/createdEvidence`。当前 RAG Handler 没有统一设置 `resultPayloadRef`，详细结果主要随 Evidence 进入 Timeline |
| `DELEGATE_AGENTS` | `mainOutput.stateDelta.delegateAgentsRequest`、`actionRequest.delegateAgentsRequest` | 初始调度信息位于 `runtimeOutcome.details.resultSnapshot`；每个子任务终态投影到 `runtimeOutcome.details.childAgentResults[childRunId]`，子任务证据合并到 `runtimeOutcome.evidenceRefs` |
| `ASK_USER` | `mainOutput.stateDelta.askUserRequest`、`actionRequest.askUserRequest` | 等待时为 `runtimeOutcome.status=WAITING_USER`；回答、Checkpoint 和恢复去向位于 `userInteraction.answer/sourceComponent/handler/checkpointPayload/continuation`。一般询问恢复后，Outcome 更新为 `USER_RESPONDED` |
| `READY_TO_DELIVER` | `mainOutput.stateDelta.deliveryRequest`、`actionRequest.deliveryRequest` | 未通过检查时为 `runtimeOutcome.details.effectStatus=DELIVERY_NOT_READY`，原因在 `runtimeOutcome.summary`；通过时主要记录 `runtimeOutcome.status/summary`，同时 Runtime 把 Main Agent 阶段切换到 `DELIVERING` |
| `FINAL` | 最终回答候选位于 `mainOutput.stateDelta.finalAnswerCandidate` 和 `actionRequest.finalAnswerCandidate` | Timeline 保存处理状态和摘要；当前最终 `finalMessageId/finalAnswerRef` 主要持久化在 Run 和最终交付记录中，没有完整投影进 `runtimeOutcome` |

&emsp;&emsp;`runtimeOutcome.status` 表示 Handler 如何驱动 Runtime，例如 `CONTINUE_LOOP`、`WAITING_USER` 或 `COMPLETED`；工具是否真正成功、RAG 是否命中等业务结果通常位于 `runtimeOutcome.details.effectStatus`。这两类状态不能混为一谈。

### 04.5. ResolvedPayloads 为什么存在

&emsp;&emsp;Timeline 为了便于持久化，不会把所有大文本直接塞进每条记录，而是通过 `resultPayloadRef` 引用 Payload。Runtime 在动作完成后读取该引用对应的真实内容，放入 `ResolvedPayloads`，结构近似：

```text
payloadRef -> 完整结果内容
```

&emsp;&emsp;这样 Main Node 既能从 Timeline 看清“哪一轮产生了哪个结果”，又能从 ResolvedPayloads 取得工具回执、检索内容或生成结果正文，而不是只看到一个无法使用的 ID。Run 在暂停、进程重启或恢复时，会从持久化存储加载完整 Timeline，再遍历每轮的 `resultPayloadRef` 重建这张映射。

### 04.6. RuntimeControl 的作用

&emsp;&emsp;模型可见的 `RuntimeControl` 包含当前 Main Agent 阶段、当前 Loop、最大 Loop、剩余 Loop，以及首轮上下文中的可用能力目录和 Token 预算。它告诉模型当前处于规划、执行还是交付阶段，还剩多少决策机会，以及可以请求哪些外部能力。真正的循环上限、权限和路由仍由 Runtime 强制执行，模型看到这些信息不等于可以修改它们。

### 04.7. 一次工具调用后的下一轮示例

&emsp;&emsp;第 0 轮中，Main Node 输出 `CALL_TOOL` 和对应的 `taskUpdate`。Runtime 先记录这次决策；Action 通过校验并进入处理器后，Runtime 合并 `taskUpdate`，通常把当前步骤记为进行中，然后校验参数与权限并调用真实工具。工具结果返回后，Runtime 把成功或失败状态、摘要、Evidence 和 `resultPayloadRef` 写入 Loop 0，同时把引用指向的结果正文解析进 ResolvedPayloads。Runtime 不会擅自把工具成功或失败改写成任务语义结论，下一轮 Main Node 读取真实 Outcome 后，再通过新的 `taskUpdate` 把步骤更新为完成、失败或改道。

&emsp;&emsp;进入第 1 轮时，Main Node 会同时看到：原始用户目标、首轮选出的背景、更新后的任务账本、Loop 0 中自己发起的 Tool Intent、Runtime 实际记录的成功或失败、结果 Evidence，以及对应 Payload 的真实内容。它据此决定继续下一步、修正参数、换工具、询问用户或准备交付。

### 04.8. 当前边界

&emsp;&emsp;首轮 `SelectedSessionContext` 有约 6000 个估算 Token 的预算，并预留约 1000 个输出 Token；工具目录也有独立的字符和结构裁剪。但是完整 LoopTimeline 和 ResolvedPayloads 当前没有再次纳入这 6000 Token 上限，也没有按需只物化与当前步骤相关的 Payload。因此当前实现保证了短中程 Run 的因果信息完整性，但长 Run 仍有上下文膨胀风险。后续需要增加 Timeline 分段摘要、Payload 按需展开、总输入 Token 预算和关键事实保留策略。

### 04.9. 给面试官的回答

&emsp;&emsp;我们的 Main Agent 上下文不是简单拼接聊天记录，而是由 Runtime 组装成一个分层的 Run Context Envelope。第一层是稳定的任务底稿，保存原始请求以及首轮经过上下文规划节点筛选出的会话、记忆、RAG、证据和工具能力；第二层是 TaskLedger，用结构化目标、交付物、步骤、事实和阻塞项表示任务当前状态；第三层是完整 LoopTimeline，逐轮分开记录模型提出的 Action 和 Runtime 真实执行结果；大结果通过引用持久化，再由 ResolvedPayloads 解析成模型可读正文；最后 RuntimeControl 告诉模型当前阶段和剩余循环预算。

&emsp;&emsp;这个设计最重要的地方是把“模型认为发生了什么”和“系统确认发生了什么”分离。每轮决策、工具调用、RAG、子 Agent 和用户交互都会由 Runtime 写入因果时间线，下一轮 Prompt 强制优先读取最新 Runtime Outcome，再结合任务账本和完整历史决定下一步。即使 Run 暂停或服务重启，也可以从持久化的基础上下文、账本、时间线和 Payload 引用恢复同一条执行链。当前需要继续补强的是长 Run 的 Timeline 压缩和总 Token 预算治理。

---

## 05. MCP 工具调用失败后如何记录、回传和重试？

### 05.1. 项目中的具体实现

&emsp;&emsp;工具失败分为调用前失败和真实调用失败。Main Action 本身缺少 `toolIntent` 或必要字段时，会先进入 Contract 处理；进入工具模块后，能力不存在、权限拒绝、参数物化失败等情况不会调用 MCP，分别形成 `TOOL_NOT_RUN` 或 `TOOL_APPROVAL_REJECTED`。参数通过初步处理后，Tool Runtime 还会用完整 Input Schema 再校验一次，不符合时把 ToolCall 标记为 `FAILED`，failureCode 为 `TOOL_SCHEMA_ERROR`。

&emsp;&emsp;真实调用阶段，MCP Client 不存在或执行器饱和时记录为未调用；超过配置超时时间返回 `MCP_TOOL_TIMEOUT`；MCP Response 的 `isError=true` 映射为 `MCP_TOOL_ERROR`；连接异常或 Client 初始化异常也会保存对应错误码。失败结果仍会生成 Tool Receipt Payload，记录 called、success、errorCode、errorMessage 和 latencyMs，并把 ToolCall 更新为 `NOT_CALLED` 或 `FAILED`。验证器随后保存失败 Verification，工具证据转换器再生成包含失败码和诊断信息的 Evidence。

&emsp;&emsp;工具处理器通常把这些失败转换为 `CONTINUE_LOOP`，因此这里的顶层 `runtimeOutcome.status` 表示 Runtime 可以继续运行，真正的工具状态位于 `runtimeOutcome.details.effectStatus=TOOL_FAILED` 或 `TOOL_NOT_RUN`。原始 Tool Intent 保存在 `actionRequest.toolIntent`，失败摘要位于 `runtimeOutcome.summary`，Evidence 引用位于 `runtimeOutcome.evidenceRefs`，完整失败 Evidence 位于 `runtimeOutcome.details.createdEvidence`；其诊断 Payload 引用进入 `resultPayloadRef` 并由 Runtime 解析到 `resolvedPayloads`。

&emsp;&emsp;下一轮 Main Node 从 `RunContextEnvelope` 读取最新 Loop，因此可以同时看到调用了什么工具、参数是什么、工具是否真正被调用、失败码、Schema 违规信息、错误摘要和诊断 Payload，再决定修正参数、换工具、询问用户、降级完成或结束任务。普通工具失败默认不直接终止整个 Run；只有工具处理模块不可用等无法安全继续的 Runtime 级故障才可能使 Run 失败。

&emsp;&emsp;当前项目没有接通确定性的工具自动重试。`RuntimeLoopPolicy.canRetryTool`、`toolRetryCount` 和递增方法虽然已经存在，但实际工具调用链没有使用它们。失败后是否再次调用主要由 Main Node 下一轮重新输出 `CALL_TOOL` 决定，并受全局最大 Loop 限制。相同 Tool Intent 已经成功时会复用既有结果并跳过重复调用；相同调用曾被用户拒绝时也会跳过；相同失败 Intent 当前仍允许模型再次请求。

&emsp;&emsp;生产化补强时，应把错误分为可重试与不可重试：连接超时、临时网络错误和服务端限流可以在满足幂等条件时进行有限次数的指数退避重试；Schema 错误、权限拒绝和用户拒绝不应自动重试；外部写操作只有具备幂等键或状态回查能力时才能自动重试，同时应增加熔断、重试次数记录和最终失败降级。

### 05.2. 给面试官的回答

&emsp;&emsp;MCP 工具失败后，我们不会只把异常抛给模型，而是把它转换成 Runtime 可持久化、可恢复的执行事实。参数错误会在完整 Schema 校验阶段阻止真实调用，并记录 `TOOL_SCHEMA_ERROR`；Client 不可用会记录为 `NOT_CALLED`；连接异常、服务端 `isError` 和超时分别保留对应错误码，其中超时为 `MCP_TOOL_TIMEOUT`。无论成功还是失败，Runtime 都会更新 ToolCall 状态，保存可用的 Receipt、Verification 和 Evidence。

&emsp;&emsp;随后工具处理器把 Tool Intent、`TOOL_FAILED` 或 `TOOL_NOT_RUN`、失败摘要、Evidence 引用和诊断 Payload 写入当前 LoopTimeline，并返回 `CONTINUE_LOOP`。下一轮 Main Node 从 RunContextEnvelope 中读取最新 Runtime Outcome 和 ResolvedPayloads，因此能区分参数问题、服务不可用、超时和服务端异常，再决定修改参数、换工具、询问用户或降级处理。

&emsp;&emsp;当前实现没有在 Runtime 中对失败工具做确定性自动重试。代码预留了重试计数和策略接口，但尚未接入实际调用链；是否重试由 Main Node 下一轮重新决策，并受最大 Loop 数约束。这样可以避免对发布、写文件等非幂等操作进行盲目重试。生产环境中会只对明确的瞬时故障，在具备幂等键的前提下增加有限次数指数退避重试，而参数、权限和用户拒绝类错误不会自动重试。

---

## 06. 为什么选择“单 Main Node 循环 + Runtime 执行器”，而不是固定多节点工作流？

### 06.1. 固定多节点架构解决不了什么

&emsp;&emsp;固定工作流适合步骤稳定、分支有限的业务，例如固定执行“检索、生成、审核、发布”。但本项目定位是通用 Agent 底座，同一个入口可能面对纯问答、RAG 检索、文件操作、MCP 外部调用、用户澄清、权限审批或子 Agent 并行委派，动作顺序和执行次数在任务开始前无法完全确定。如果把这些能力都固化成节点，流程图会随着能力增加产生大量条件分支，新接入一种能力也可能需要修改整张工作流。

&emsp;&emsp;项目旧的固定 Node1-4 设计已经暴露出具体问题：节点职责边界不清晰、节点间协调不稳定、中间 Payload 过长、共享上下文逐渐变成无边界对象、验证或过程文本可能污染最终回答，而且很难继续扩展工具、RAG、Artifact、用户确认和未来编码能力。多节点并不天然意味着模块化；如果多个 LLM 节点都能解释和修改任务状态，反而会产生目标漂移、重复规划和状态所有权不清。

### 06.2. 当前架构怎样解决这些问题

&emsp;&emsp;当前设计把系统分成“一个语义决策中心”和“一组确定性执行模块”。Main Node 负责理解完整用户目标、维护 TaskLedger，并在每轮只选择一个结构化 Action；Runtime 负责状态机、Contract 校验、Action 路由、权限审批、真实工具调用、结果验证、持久化、暂停恢复和最终交付。RAG、Tool、ASK_USER、子 Agent 和 Final Delivery 仍是独立模块，但它们是由 Runtime 路由的能力处理器，不是依次强制经过的固定 LLM 节点。

```text
Main Node 读取 RunContextEnvelope
  -> 更新 TaskLedger 并选择一个 Action
  -> Runtime 校验并执行
  -> Runtime 将真实 Outcome 写入 LoopTimeline
  -> Main Node 根据新事实继续、改道或交付
```

&emsp;&emsp;这种结构让简单任务可以很快进入交付，复杂任务则可以按需要多轮调用不同能力。新接入 MCP 工具时，主要扩展能力目录和 Runtime 执行配置，不需要增加一个固定工作流节点。所有步骤仍围绕同一份原始目标、TaskLedger 和 Timeline 推进，避免不同 LLM 节点分别维护彼此不一致的任务理解。

### 06.3. One-Loop PER 相比三个 LLM 节点循环的核心优势

&emsp;&emsp;如果把 Plan、Execute、Replan 分别设计为三个 LLM 节点，任务语义必须在节点之间不断交接：Planner 把用户目标压缩成计划，Executor 再解释计划并产生动作，Replanner 又根据 Executor 或 Observer 的摘要重新理解任务。每一次交接都是一次新的概率性解释，容易发生信息丢失和语义漂移。Planner 关注全局但缺少真实执行细节，Executor 关注当前步骤但可能忽略整体交付物，Replanner 看到了结果却不一定理解 Planner 当时为什么这样设计。

&emsp;&emsp;多节点上下文设计会陷入两难：如果给三个节点都注入原始问题、完整历史和工具结果，就会重复消耗大量 Token，而且三个节点可能对同一事实形成不同理解；如果每个节点只收到裁剪后的局部上下文，就必须依赖节点间摘要，而摘要本身会成为有损压缩，遗漏用户限制、计划理由、失败细节或验收条件。

&emsp;&emsp;One-Loop PER 将 Planning 和 Replanning 交给同一个 Main Node 角色。LLM 调用本身仍然无状态，但每轮都使用同一套角色边界和 Action Contract，重新读取 Runtime 维护的原始 userInput、当前 TaskLedger、完整 LoopTimeline 和 ResolvedPayloads。它得到的是一手规范化状态，而不是另一个 LLM 节点加工后的二手理解，因此能够持续围绕同一目标判断计划是否有效。

&emsp;&emsp;更关键的是，项目没有再设置一个 LLM Executor。Main Node 在一次输出中同时产生 `taskUpdate` 和下一步 `action`，使“我认为任务状态怎样变化”和“我接下来准备执行什么”成为同一个原子决策；真正的 Execute 由 Runtime 根据 Action 确定性完成。Runtime 不会重新解释任务意图，只负责 Contract、权限、Schema、路由和真实执行。执行结果也不经过另一个 Observer LLM 总结，而是以 Runtime Outcome、Evidence 和 Payload 原样进入 Timeline，再交给下一轮 Main Node。

| 对比维度 | 三个 LLM 节点的 Plan/Execute/Replan | One-Loop PER + Runtime |
| --- | --- | --- |
| 任务理解 | 多个节点分别解释，容易目标漂移 | 同一 Main Node 角色持续维护任务语义 |
| 上下文 | 全量注入会重复，局部注入会丢失 | 每轮读取同一份规范化 RunContextEnvelope |
| 计划与动作 | Planner 的计划可能被 Executor 二次解释 | `taskUpdate + action` 在同一次输出中原子产生 |
| 执行准确性 | LLM Executor 可能选错工具、改写参数或虚构成功 | Runtime 使用注册表、Schema 和权限规则确定性执行 |
| 结果观察 | Observer/Replanner 可能只收到结果摘要 | Main Node 直接读取 Runtime Outcome、Evidence 和 Payload |
| 误差传播 | 多个 LLM 边界形成连续概率误差 | 只保留一个核心语义决策边界，其余尽量确定化 |
| Token 成本 | 同一背景可能在多个节点重复注入 | 每个 Loop 只调用一次核心决策节点 |

&emsp;&emsp;因此它对任务准确度的提升，不是因为“一个节点一定比三个节点聪明”，而是因为减少了 LLM 之间的语义翻译次数，让计划更新与动作选择保持一致，并把可以确定执行的部分从概率模型中移出。它解决的是认知一致性和误差链长度问题。

### 06.4. 它与 Plan-Execute-Replan、ReAct 的关系

&emsp;&emsp;项目更准确的描述是“Runtime 治理的 Plan-Execute-Replan 主循环”。首轮 Main Node 在 `PLANNING` 阶段建立目标、交付物和初始步骤；Runtime 执行一个 Action；后续 Main Node 在 `EXECUTING` 阶段读取真实 Outcome，保留仍然有效的计划或通过 `planRevision` 改道；全部交付物就绪后，Runtime 才允许进入 `DELIVERING` 阶段生成最终回答。

&emsp;&emsp;它并不是与 ReAct 完全相反。在“模型决策 Action、环境执行、结果作为 Observation 回到下一轮”这一节奏上，它具有 ReAct 特征；区别在于没有采用纯 Prompt 驱动的裸 ReAct。模型不能自由修改运行状态或直接宣称工具执行结果，而是输出受 Contract 约束的 JSON Action；Observation 来自 Runtime 持久化的真实 Outcome；同时系统还有显式 TaskLedger、状态机、权限审批、Checkpoint、Evidence 和最终交付 Guard。

&emsp;&emsp;Claude Code 这类 Coding Agent 可以作为产品形态上的启发：通用任务更适合让核心模型在工具结果反馈下持续决定下一步，而不是固定经过 Planner、Searcher、Writer 等角色。但面试中不应声称复制了 Claude Code 的内部架构，因为其完整实现并不是项目的设计依据。更稳妥的说法是，项目借鉴了 Coding Agent 常见的 Agentic Tool-Use Loop 思路，并自行实现了 Java Runtime、结构化 Action、持久化上下文和安全治理。

### 06.5. 这项选择的代价

&emsp;&emsp;单 Main Node 循环不是在所有场景都优于固定工作流。它会让 Main Node Prompt 和决策职责更重，完整 Timeline 可能造成上下文膨胀，模型决策错误还可能连续影响后续循环；所有任务都依赖同一个决策中心，也形成一定性能和质量瓶颈。当前项目通过结构化 Contract、TaskLedger、最大 Loop、Runtime Guard 和专用辅助节点缓解这些问题，后续还需要补充 Timeline 压缩、按需 Payload 展开和更细粒度的效果评测。

&emsp;&emsp;如果业务就是固定、强合规且高频重复的流程，例如标准审批或固定报表生成，固定工作流通常更可控、成本更低。本项目选择主循环，是因为目标是能力可扩展的通用 Agent 底座，而不是因为主循环在所有任务上都更先进。

### 06.6. 给面试官的回答

&emsp;&emsp;我们选择 One-Loop PER 的核心原因，不只是为了流程更灵活，而是为了减少多个 LLM 节点之间的语义交接。如果把 Planner、Executor 和 Replanner 分成三个模型节点，原始任务、计划和执行结果需要被反复摘要和重新解释。给每个节点全量上下文会造成 Token 重复和多套理解，给局部上下文又会产生信息损失，最终容易出现 Planner 的目标、Executor 的动作和 Replanner 的判断不一致。

&emsp;&emsp;在我们的设计里，Planning 和 Replanning 由同一个 Main Node 角色承担。虽然每次 LLM 调用仍然无状态，但它每轮都读取同一份由 Runtime 维护的原始 userInput、TaskLedger、完整 LoopTimeline 和 ResolvedPayloads，不依赖其他 LLM 节点加工后的二手上下文。Main Node 还会在一次输出中同时给出 `taskUpdate` 和 `action`，保证任务状态更新和下一步动作来自同一个判断。

&emsp;&emsp;真正的 Execute 不由第二个 LLM 节点完成，而由 Runtime 根据结构化 Action 确定性执行；Observation 也不是另一个节点生成的自然语言摘要，而是经过验证的 Runtime Outcome、Evidence 和 Payload。这样减少了概率性决策边界，避免 Executor 改写计划、选错能力或者虚构执行结果。它在 Action-Observation 节奏上与 ReAct 相似，但本质上是一个由 Runtime 治理、具有显式 TaskLedger 和可恢复状态的 Plan-Execute-Replan 主循环。

&emsp;&emsp;所以 One-Loop 的优势不是“节点越少越好”，而是让一个节点持续拥有任务语义，把执行与验证确定化。对于固定、强流程的业务，我仍然会使用 Workflow；但对于工具集合和执行路径动态变化的通用 Agent，这种结构在上下文一致性、计划执行一致性和错误控制上更合适。

---

## 07. Main Node 输出的结构化 Action 不合法时如何处理？

### 07.1. 先说结论：有限自愈，失败关闭

&emsp;&emsp;项目不是遇到任意格式错误就立即终止，也不会让模型无限重新生成。完整策略是：先在模型调用管线内做确定性解析和校验；能够归类为结构问题时，最多进行一次受限的 Contract 修复调用；修复结果仍然要重新通过同一套解析和校验。修复仍失败后，Java 代码会生成一个合法的 `FAIL` Action，由 Runtime 按正常失败交付链安全结束 Run。

&emsp;&emsp;这里需要区分两个职责。模型调用管线负责把“不稳定的模型文本”转换成“可信的 Java Action”；Runtime 负责判断这个 Action 在当前执行阶段是否允许、是否有对应处理器，以及能否真正执行。因此，模型原始文本通常不会未经处理直接进入 Runtime。

```text
Main Node 原始输出
  -> JSON 规范化与解析
  -> Java 类型映射
  -> Action Contract 内容校验
  -> 校验成功：返回 MainAgentActionVO
  -> 校验失败：最多一次 Contract Repair LLM 调用
       -> 对修复结果重新解析、映射和校验
       -> 成功：返回修复后的 Action
       -> 仍失败：Java 构造合法 FAIL Action
  -> Runtime 二次校验 Action 类型、阶段、Contract、权限和 Handler
  -> 执行 Action，或者安全终止 Run
```

### 07.2. 第一层：模型调用管线如何把文本变成 Action

&emsp;&emsp;`NodeInvocationPipeline` 可以理解为所有结构化 LLM 节点共用的“调用与验收管线”。Main Node 调用模型后，它先通过 `RawOutputParser` 处理原始文本。解析器会去掉允许的 Markdown JSON 代码围栏，并对少量可确定修复的问题做本地修正，例如补齐有限数量的末尾对象括号、修正非法字符串转义。它要求最终结果必须是且只能是一个 JSON 对象，因此 JSON 后面附带解释文字也会被拒绝。

&emsp;&emsp;JSON 解析成功后，`NodeOutputMapper` 将对象映射为项目中的 Action 数据对象。这里会先检查 `action` 是否属于已注册枚举，因此缺少 `action` 或输出不存在的动作名，会形成 `MAPPING_FAILED`。随后 `ContractValidator` 继续做内容级校验，主要包括：

- `action` 必须属于 Main Agent v2 允许的动作集合。
- 必须携带 `taskUpdate` 和 `stateDelta`。
- 每种 Action 必须携带自己的 Payload，例如 `CALL_TOOL` 必须有 `toolIntent`，`FINAL` 必须有最终内容。
- Payload 中的必要字段必须存在，例如工具调用需要 `capabilityCode`、`toolName`、`goal` 和 `arguments`。
- Action 只能写入允许的 `stateDelta` 字段，不能写入 `runStatus` 等 Runtime 自己管理的生命周期字段。
- 任务步骤状态、ASK_USER 输入模式、子 Agent 委派参数等业务枚举和组合必须合法。

&emsp;&emsp;因此，“能被 JSON 解析”并不等于“能执行”。例如下面的输出虽然是合法 JSON，但仍然会因为缺少 `toolIntent` 而被 Contract 拒绝：

```json
{
  "action": "CALL_TOOL",
  "taskUpdate": {},
  "stateDelta": {}
}
```

### 07.3. 校验失败后如何让模型修复

&emsp;&emsp;当前 Main Node 的模型绑定将 `maxRepairAttempts` 配置为 `1`，所以最多只允许一次修复。这个修复不是重新执行完整 Main Node，也不是让模型重新规划用户任务，而是一次额外、职责受限的 LLM 调用。修复请求只包含原组件、Contract 版本、错误的原始输出、具体错误码与字段路径，以及当前修复次数。

&emsp;&emsp;修复 Prompt 明确要求模型只能修 JSON 语法、缺失字段、禁止字段和 `stateDelta` 作用域等结构问题，不能重新规划、不能调用工具，也不能添加 Runtime 生命周期字段，只能输出修正后的 JSON。修复输出不会被直接信任，而是重新经过同一套“解析、类型映射、Contract 校验”。只有第二次验收成功，调用结果才会标记为 `REPAIR_SUCCEEDED` 并交给 Runtime。

&emsp;&emsp;空输出是一个单独分支。系统会使用原始 Prompt 重新调用 Main Node 一次，因为空输出中没有可供 Contract Repair 修正的内容；如果第二次仍为空，则以 `PARSE_FAILED / EMPTY_OUTPUT` 结束节点调用，不会要求修复模型凭空编造 Action。模型接口异常、Prompt 超限等 Client Error 也不会进入 Contract Repair，而是直接按调用失败处理。

### 07.4. 修复耗尽后 Runtime 如何结束

&emsp;&emsp;如果非法 JSON、映射错误或 Contract 违规在一次修复后仍未解决，调用管线返回 `REPAIR_FAILED`，并且不会提供可执行的类型化结果。Main Node 服务检测不到合法 Action 后，不会把坏 JSON 交给 Runtime，而是由 Java 构造一个固定格式的 `FAIL` Action，其中包含失败码、安全用户提示和仅供开发排查的错误信息。

&emsp;&emsp;Runtime 收到这个 `FAIL` Action 后，仍然按照正常流程进入 `VALIDATING_ACTION`。它先确认 Action 类型存在，再由动作分发器检查当前阶段是否允许 `FAIL`、重新执行一次 Main Action Contract 校验、检查能力权限并查找对应 Handler。`FAIL` 在规划、执行和交付阶段都被允许，最终由失败动作处理器经过 Final Delivery 交付安全提示，并把 Run 置为 `FAILED`。

&emsp;&emsp;Runtime 自己还保留了第二道拒绝防线：如果由于代码调用、反序列化或其他非正常路径，收到空 Action、未知 `actionType`、当前阶段不允许的 Action、再次校验失败，或者根本找不到处理器，它不会再让模型修复，而是直接生成 `MAIN_ACTION_CONTRACT_FAILED` 或 `ACTION_HANDLER_UNAVAILABLE` 等安全失败并结束 Run。正常链路中，这道防线很少命中，但它保证任何未验收动作都不能被执行。

### 07.5. 不同错误的实际处理结果

| 错误情况 | 第一处理方式 | 最终仍失败时 |
| --- | --- | --- |
| 首次返回空字符串 | 使用原 Prompt 重调 Main Node 一次 | `PARSE_FAILED / EMPTY_OUTPUT`，转为 Java `FAIL` Action |
| JSON 语法错误 | 本地做有限确定性修正；仍失败则进入一次 Contract Repair | `REPAIR_FAILED`，转为 Java `FAIL` Action |
| 缺少必要字段 | Contract 返回错误码和字段路径，进入一次 Contract Repair | 转为 Java `FAIL` Action |
| `actionType` 不存在 | 类型映射形成 `MAPPING_FAILED`，进入一次 Contract Repair | 转为 Java `FAIL` Action |
| Action 与 Payload 不匹配 | Contract 拒绝并进入一次 Contract Repair | 转为 Java `FAIL` Action |
| 模型接口异常或 Prompt 超限 | 不做 Contract Repair，直接返回 Client Failure | 转为 Java `FAIL` Action |
| 非正常路径绕过调用管线，Runtime 收到非法 Action | Runtime 或动作分发器直接拒绝 | Run 安全失败，不再调用模型修复 |

### 07.6. 这样设计的原因

&emsp;&emsp;一次受限修复可以吸收大模型偶发的括号、字段遗漏和枚举输出错误，避免本来可以完成的任务因为轻微格式问题直接失败；限制修复次数又可以避免模型反复输出错误造成无限循环和 Token 消耗。更重要的是，修复过程只调整 Contract 结构，不重新决定任务语义，防止修复节点借机改变计划或执行意图。

&emsp;&emsp;最后使用 Java 构造的固定 `FAIL` Action，而不是直接抛异常退出，可以让失败也走统一的 Action 记录、Runtime 状态迁移、Transcript、Final Delivery 和用户安全提示链路。整个策略可以概括为：对可恢复的格式错误允许一次自愈，对无法证明合法的动作一律拒绝执行。

### 07.7. 给面试官的回答

&emsp;&emsp;我们不是让 Runtime 直接处理模型返回的原始字符串，而是在 Main Node 和 Runtime 之间放了一条统一的结构化调用管线。模型返回后先做 JSON 规范化和解析，再映射成 Java Action，并根据 Java 代码定义的 Contract 校验 Action 类型、`taskUpdate`、`stateDelta`、各 Action 专属 Payload、必填字段和写入边界。比如 `CALL_TOOL` 必须携带完整 `toolIntent`，模型也不能写入 `runStatus` 这类 Runtime 所有的字段。

&emsp;&emsp;如果是非法 JSON、字段缺失、未知 Action 或 Payload 作用域错误，系统会把原始输出、错误码和字段路径交给一个受限的 Contract Repair 调用。它不是重新规划任务，只负责修正结构，而且当前最多修复一次；修复后的 JSON 还要重新通过同一套解析、映射和 Contract 校验。空响应则使用原 Prompt 重试一次，因为空内容不能让修复模型凭空补写；接口异常等 Client Error 不进入结构修复。

&emsp;&emsp;如果修复后仍不合法，坏输出不会进入 Runtime。Java 会生成一个格式正确的 `FAIL` Action，Runtime 按正常动作链完成记录、安全交付并把 Run 置为失败。Runtime 自身还有第二道校验，会再次检查 Action 类型、当前阶段是否允许、Contract、权限和 Handler；这一层如果失败会直接安全终止，不再让模型无限重试。所以我们的原则是“有限修复、重新验收、失败关闭”，既提高大模型偶发格式错误的容错率，又保证任何未通过 Contract 的动作都不会被执行。
