<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="yhx.com.infrastructure.dao.IAgentLongTermMemoryDao">

    <resultMap id="AgentLongTermMemoryMap" type="yhx.com.infrastructure.dao.po.AgentLongTermMemoryPO">
        <id column="id" property="id"/>
        <result column="memory_id" property="memoryId"/>
        <result column="user_id" property="userId"/>
        <result column="session_id" property="sessionId"/>
        <result column="memory_type" property="memoryType"/>
        <result column="summary" property="summary"/>
        <result column="content_ref" property="contentRef"/>
        <result column="score" property="score"/>
        <result column="status" property="status"/>
        <result column="source_run_id" property="sourceRunId"/>
        <result column="source_turn_id" property="sourceTurnId"/>
        <result column="last_seen_at" property="lastSeenAt"/>
        <result column="disabled_at" property="disabledAt"/>
        <result column="superseded_by" property="supersededBy"/>
        <result column="metadata_json" property="metadataJson"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <insert id="insert" parameterType="yhx.com.infrastructure.dao.po.AgentLongTermMemoryPO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO agent_long_term_memory (
            memory_id, user_id, session_id, memory_type, summary, content_ref, score,
            status, source_run_id, source_turn_id, last_seen_at, disabled_at, superseded_by, metadata_json,
            created_at, updated_at
        ) VALUES (
            #{memoryId}, #{userId}, #{sessionId}, #{memoryType}, #{summary}, #{contentRef}, #{score},
            #{status}, #{sourceRunId}, #{sourceTurnId}, #{lastSeenAt}, #{disabledAt}, #{supersededBy}, #{metadataJson},
            #{createdAt}, #{updatedAt}
        )
    </insert>

    <select id="queryByMemoryId" resultMap="AgentLongTermMemoryMap">
        SELECT id, memory_id, user_id, session_id, memory_type, summary, content_ref, score,
               status, source_run_id, source_turn_id, last_seen_at, disabled_at, superseded_by, metadata_json,
               created_at, updated_at
        FROM agent_long_term_memory
        WHERE memory_id = #{memoryId}
        LIMIT 1
    </select>

    <select id="listCandidates" resultMap="AgentLongTermMemoryMap">
        SELECT id, memory_id, user_id, session_id, memory_type, summary, content_ref, score,
               status, source_run_id, source_turn_id, last_seen_at, disabled_at, superseded_by, metadata_json,
               created_at, updated_at
        FROM agent_long_term_memory
        WHERE user_id = #{userId}
          AND status = 'ACTIVE'
          AND (session_id = #{sessionId} OR session_id IS NULL OR session_id = '')
        ORDER BY score DESC, updated_at DESC
        LIMIT #{limit}
    </select>

    <select id="listActiveByUser" resultMap="AgentLongTermMemoryMap">
        SELECT id, memory_id, user_id, session_id, memory_type, summary, content_ref, score,
               status, source_run_id, source_turn_id, last_seen_at, disabled_at, superseded_by, metadata_json,
               created_at, updated_at
        FROM agent_long_term_memory
        WHERE user_id = #{userId}
          AND status = 'ACTIVE'
          AND (session_id = #{sessionId} OR session_id IS NULL OR session_id = '')
        ORDER BY updated_at DESC, score DESC
        LIMIT #{limit}
    </select>

    <select id="listActiveBySession" resultMap="AgentLongTermMemoryMap">
        SELECT id, memory_id, user_id, session_id, memory_type, summary, content_ref, score,
               status, source_run_id, source_turn_id, last_seen_at, disabled_at, superseded_by, metadata_json,
               created_at, updated_at
        FROM agent_long_term_memory
        WHERE session_id = #{sessionId}
          AND status = 'ACTIVE'
        ORDER BY updated_at DESC, score DESC
        LIMIT #{limit}
    </select>

    <update id="updateLifecycle">
        UPDATE agent_long_term_memory
        SET status = #{status},
            superseded_by = #{supersededBy},
            disabled_at = CASE WHEN #{status} = 'DISABLED' THEN CURRENT_TIMESTAMP ELSE disabled_at END,
            updated_at = CURRENT_TIMESTAMP
        WHERE memory_id = #{memoryId}
    </update>

</mapper>
