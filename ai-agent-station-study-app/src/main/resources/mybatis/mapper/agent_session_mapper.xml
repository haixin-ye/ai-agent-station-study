<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="yhx.com.infrastructure.dao.IAgentSessionDao">

    <resultMap id="AgentSessionMap" type="yhx.com.infrastructure.dao.po.AgentSessionPO">
        <id column="id" property="id"/>
        <result column="session_id" property="sessionId"/>
        <result column="user_id" property="userId"/>
        <result column="title" property="title"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <insert id="insert" parameterType="yhx.com.infrastructure.dao.po.AgentSessionPO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO agent_session (session_id, user_id, title, status, created_at, updated_at)
        VALUES (#{sessionId}, #{userId}, #{title}, #{status}, #{createdAt}, #{updatedAt})
    </insert>

    <select id="queryBySessionId" parameterType="java.lang.String" resultMap="AgentSessionMap">
        SELECT id, session_id, user_id, title, status, created_at, updated_at
        FROM agent_session
        WHERE session_id = #{sessionId}
    </select>

</mapper>
