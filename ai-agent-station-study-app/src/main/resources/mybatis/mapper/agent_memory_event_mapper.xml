<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="yhx.com.infrastructure.dao.IAgentMemoryEventDao">

    <insert id="insert" parameterType="yhx.com.infrastructure.dao.po.AgentMemoryEventPO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO agent_memory_event (
            event_id, run_id, session_id, memory_id, event_type, payload_ref, created_at
        ) VALUES (
            #{eventId}, #{runId}, #{sessionId}, #{memoryId}, #{eventType}, #{payloadRef}, #{createdAt}
        )
    </insert>

</mapper>
